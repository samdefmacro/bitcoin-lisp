# GA10 — full verdict table (rounds 3 and 4)

Every finding, its final severity, and whether the verdict was reached by executing code.
`refuted` means a skeptic killed it; nothing here is un-judged.

| id | final | exec | finding |
|---|---|---|---|
| `2279d91b` | **S1** | Y | coins-view-cache-sync clears DIRTY but leaves FRESH set, so coins already written to LevelDB are later dropped on spend without an eras |
| `2f0cf648` | **S2** | Y | Outbound netgroup-diversity set is built from ALL peers including inbound, so free inbound connections can veto our outbound replacemen |
| `42dacfaf` | **S2** | Y | Every dial counts an addrman failure — Core suppresses failure counting when the node looks offline, and never counts it for manual/add |
| `56b53a04` | **S2** | Y | getnetworkhashps ignores its `height` argument entirely, does not implement nblocks = -1, and never validates its inputs |
| `7f01522e` | **S2** | Y | %scan-flat-block-files stops at the first missing blk file, so a pruned node loses its entire flat block index on restart |
| `801f2ad3` | **S2** | Y | -rpcpassword / -rpcauth / -rpcuser / -torpassword are written to debug.log in cleartext; Core masks them as **** |
| `87801e86` | **S2** | Y | Network-only options (-port, -rpcport, -bind, -connect, -addnode, -wallet, -walletdir) are honored from bitcoin.conf's default section  |
| `97855c61` | **S2** | Y | PSBT "complete" and the already-signed test trust final/partial fields without verifying the scripts |
| `abae237b` | **S2** | Y | Eviction's disadvantaged-network reserve protects the NEWEST connections instead of the longest-connected (comparator inverted) |
| `c831e584` | **S2** | Y | The RPC coins sync advances the persisted coins best-block pointer without persisting the block index, so a later crash makes the node  |
| `d2099b36` | **S2** | Y | The "-peerblockfilters without -blockfilterindex" refusal is nested inside (when prune ...), so an unpruned node advertises NODE_COMPAC |
| `d4f123e8` | **S2** | Y | A negated repeatable option (-noX) is appended to the list as the literal string "0" instead of clearing it; -norpcauth kills the RPC s |
| `d9aadbc5` | **S2** | Y | IsBadPort is absent: automatic outbound dials will connect to any gossiped port (SMTP, SSH, MySQL, IRC, …) |
| `e2db2089` | **S2** | Y | -rpcwhitelist / -rpcwhitelistdefault are accepted at startup but never enforced, so a user the operator restricted gets full RPC |
| `feac3eb3` | **S2** | Y | /rest/spenttxouts serializes the on-disk compressed CBlockUndo codec instead of Core's REST format, and its JSON omits Core's leading c |
| `0a061de4` | **S3** | Y | getrawmempool ignores its second argument mempool_sequence, which our own argument table advertises |
| `0c05f5d0` | **S3** | n | Relayed addresses go out immediately, one addr message per address, instead of Core's per-peer Poisson-batched queue |
| `0c63c99c` | **S3** | Y | CheckTxInputs (coinbase maturity, fee/value consensus) runs after the standardness-of-inputs, sigop and witness checks instead of befor |
| `0e7ec2f3` | **S3** | Y | /rest/mempool/contents.json ignores the ?verbose= and ?mempool_sequence= query parameters and always serves the verbose dump |
| `105290bf` | **S3** | Y | Dotted section keys in bitcoin.conf (main.rpcport=..., test.connect=...) are treated as unknown options and dropped |
| `24e0c216` | **S3** | Y | /rest/block/<hash>.json renders getblock verbosity 2; Core renders verbosity 3 (SHOW_DETAILS_AND_PREVOUT) |
| `272e30f0` | **S3** | Y | CONST_SCRIPTCODE's OP_CODESEPARATOR rule is applied only to the scriptPubKey and to a scriptCode reached by a CHECKSIG, not to the scri |
| `2834e5b3` | **S3** | Y | JSON-RPC batch members never get the named-argument transform, so any batch call using named params fails with -32603 |
| `32758a48` | **S3** | Y | A negative -maxconnections is accepted and clamped instead of being an init error |
| `34a807d1` | **S3** | Y | ECDSA satisfaction sized at 72 bytes on a stated justification that is factually false — our signer does grind low-R |
| `38bb5cc7` | **S3** | Y | CheckTxInputs' input-value MoneyRange guards and ConnectBlock's accumulated-fee guard have no analogue |
| `3911beba` | **S3** | Y | -bind / -whitebind together with an explicit -listen=0 is accepted; Core makes it a hard init error |
| `3f90b8bc` | **S3** | Y | getmininginfo reports currentblockweight/currentblocktx as 0 before any template has been assembled; Core omits the fields |
| `5054e381` | **S3** | Y | wtxidrelay / sendaddrv2 received after VERACK are silently ignored where Core disconnects |
| `508fedab` | **S3** | Y | PSBT parser omits two of Core's global-map validity checks (out-of-range prevout index, unsupported PSBT version) |
| `54fdda5c` | **S3** | Y | Duplicate-in-mempool check is txid-only: Core's "txn-same-nonwitness-data-in-mempool" case is never reported |
| `57e206a8` | **S3** | Y | The BIP94 timewarp floor and check use the fixed 2016-block interval and a hardcoded testnet4 gate; the comment justifying this misread |
| `631b90f9` | **S3** | Y | -forcednsseed=1 with -dnsseed=0 (or with -connect) is silently ignored instead of being an init error |
| `6635e4c9` | **S3** | Y | Package consistency check adds inputs one at a time, misreporting a transaction with duplicate inputs as a package conflict; empty-vin  |
| `70502bf3` | **S3** | Y | A backed-up send buffer makes us DROP outbound messages; Core instead stops processing inbound messages until it drains |
| `7b813f60` | **S3** | Y | generatetoaddress / generatetodescriptor raise an error when maxtries is exhausted instead of returning the blocks mined so far |
| `7db4b27f` | **S3** | Y | verify-checksig's inline FindAndDelete pattern omits the PUSHDATA prefix for signatures longer than 75 bytes |
| `81c98c4b` | **S3** | Y | enforce_BIP94 is hardcoded to testnet4; Core lets regtest turn it on with -test=bip94 |
| `83599047` | **S3** | Y | Block templates never signal any BIP9 deployment: nVersion is the hardcoded constant 0x20000000 and vbavailable is hardcoded empty |
| `88f3a071` | **S3** | Y | Block-conflict removal does not clear the conflicted transaction's prioritisation delta |
| `8d88f6ac` | **S3** | Y | base58-decode has no length bound; Core's DecodeBase58 bails as soon as the decoded length exceeds max_ret_len |
| `98069fe2` | **S3** | Y | sendcmpct(0) never clears the recorded high-bandwidth-from flag |
| `994f223b` | **S3** | Y | The relay-finality check runs after the duplicate and missing-input checks, so a non-final transaction with unknown parents is stored i |
| `9f54cae8` | **S3** | n | %reorg-commit fires tx-relay and wallet side effects without connect-block's background-chainstate role guard |
| `a4680ae1` | **S3** | Y | Gossiped addresses are relayed immediately, one addr message per address — no m_addrs_to_send queue and no poisson broadcast timer |
| `a6afe2d6` | **S3** | Y | generateblock builds its own coinbase and re-introduces the unconditional segwit witness that builder.lisp already fixed |
| `a7b67ad1` | **S3** | Y | OP_CHECKLOCKTIMEVERIFY / OP_CHECKSEQUENCEVERIFY consult DISCOURAGE_UPGRADABLE_NOPS when their own flag is off; Core treats them as a pl |
| `a9ddbd92` | **S3** | Y | getnetworkhashps computes the wrong number: integer rounding drops sub-1 H/s results to 0, and the timespan uses the window's endpoints |
| `b219c779` | **S3** | Y | getrawtransaction consults the mempool even when an explicit blockhash is given, and never rejects an unknown blockhash |
| `b48227fc` | **S3** | Y | FindAndDelete is skipped for an EMPTY signature; Core's pattern for an empty sig is the single byte 0x00 (OP_0), not the empty script |
| `ba446c02` | **S3** | Y | Block script validation never consults the script-execution cache, so every mempool-verified transaction is re-interpreted at connect |
| `cc698623` | **S3** | Y | No REST endpoint checks RPC warmup, so /rest/* serves a half-initialized node during startup instead of 503 |
| `ccac3137` | **S3** | Y | The intra-block coin overlay keeps provably-unspendable outputs that Core's AddCoin drops |
| `d410b7bf` | **S3** | Y | script-is-push-only-p rejects OP_RESERVED (0x50); Core's IsPushOnly deliberately counts it as push-type |
| `d73be57b` | **S3** | Y | Rolling minimum fee decays with no block since the bump, and block connection never resets the decay clock |
| `d8cffd5c` | **S3** | n | PSBT signing never checks that existing signatures use the requested sighash type |
| `df63423d` | **S3** | Y | Gossiped addresses are stored in addrman and relayed onward without the banned/discouraged filter Core applies at ingest |
| `f506ca73` | **S3** | n | No -walletcrosschain guard: a wallet whose locator belongs to another chain is loaded and rewritten |
| `f5b31fb7` | **S3** | Y | OP_CHECKMULTISIG charges nKeysCount against the 201-op budget only after running the signature verifications; Core charges and enforces |
| `fac08286` | **S3** | Y | Coinbase "hash" (wtxid) reported as 32 zero bytes by the RPC transaction serializer; Core reports the real witness hash |
| `fdbe8a5e` | **S3** | Y | The prune window's lower bound is our walk cursor rather than Core's 0, so the first blk file is never prunable |
| `ff51e9d5` | **S3** | Y | Inbound admission ban/discourage checks have no NoBan permission exemption |
| `refactor-1` | **S3** | Y | fsync-parent-directory was written to fix a durability bug and wired into only 3 of its 7 drive sites — 4 atomic-rename paths still nev |
| `refactor-2` | **S3** | Y | bl:token-bucket and bl:make-token-bucket are now undefined symbols — a real regression from the util-layer split |
| `refactor-3` | **S3** | Y | known-config-option-p silently lost its case-insensitive fallback for core-only options |
| `606ac3e5` | **refuted** | Y | address-book-add is missing Core's "do not update if no new information is present" gate and its nTime refresh interval |
| `6dcf6d71` | **refuted** | Y | Block-relay-only eviction protection ignores fRelevantServices, so a non-tx-relay peer without the services we want can take one of the |

## Evidence

### `0a061de4` — S3

**getrawmempool ignores its second argument mempool_sequence, which our own argument table advertises**

CONFIRMED, executed. Ours: src/rpc/mempool.lisp:113-142 binds only `(%positional-bool (first params))`; `(second params)` is never read, and there is no verbose+sequence conflict check. The argument IS advertised: src/rpc/core-tables.lisp:63 `("getrawmempool" (0 "verbose") (1 "mempool_sequence"))` and core-tables.lisp:279 `("getrawmempool" "verbose" "mempool_sequence")`. Core: mempool.cpp:571-605 MempoolToJSON returns {"txids":[...],"mempool_sequence":N} for verbose=false+sequence=true (sequence read under the same pool.cs), and throws RPC_INVALID_PARAMETER "Verbose results cannot contain mempool sequence values." for verbose=true+sequence=true (mempool.cpp:574-576); the handler reads params[1] at mempool.cpp:693-698.
EXECUTED: on a test node ->
  (rpc-getrawmempool node (list nil t)) => #()            ; bare array, no object, no sequence
  (rpc-getrawmempool node (list t t))   => empty hash-table ; no error where Core raises -8
  (%named-params-to-positional "getrawmempool" {verbose:nil, mempool_sequence:t}) => (NIL T) then result #()
Positive control that the table is what accepts it: the same call with "bogus_arg" signals "RPC Error -8: Unknown named parameter bogus_arg". So a named or positional mempool_sequence=true is accepted, mapped to slot 1, and silently dropped. The counter itself exists and is live (bl.mp:mempool-sequence, src/mempool/mempool.lisp:430, read by src/zmq.lisp:261-274 and src/networking/protocol.lisp:2880/2930), so the ZMQ-snapshot workflow the argument exists for is genuinely reachable here. No comment documents the omission. S3 unchanged: an RPC-surface gap affecting external mempool-mirroring clients, not consensus.

### `0c05f5d0` — S3

**Relayed addresses go out immediately, one addr message per address, instead of Core's per-peer Poisson-batched queue**

MECHANISM CONFIRMED BY READING BOTH TREES. Core: RelayAddress -> PushAddress only appends to peer.m_addrs_to_send; MaybeSendAddr (net_processing.cpp:5570-5599) returns early while `current_time <= peer.m_next_addr_send`, redraws that deadline as rand_exp_duration(AVG_ADDRESS_BROADCAST_INTERVAL) (=30s, :160), then flushes the WHOLE accumulated vector as ONE addr/addrv2. Ours: RELAY-ADDRESS (protocol.lisp:1108-1159) builds `(build-addr-response p (list peer-addr))` and SEND-MESSAGEs it inside the per-target loop at :1157, driven from %PROCESS-GOSSIPED-ADDRESSES's `(loop for (pa . max-targets) in relay-candidates do (relay-address ...))` at :1337-1339, i.e. synchronously inside the ADDR handler. No queue exists: `grep -rn 'addrs-to-send|next-addr-send|maybe-send-addr' src/` returns nothing. The only Poisson timer on this side is PEER-NEXT-LOCAL-ADDR-SEND (peer.lisp:152), which serves the ~24h SELF-announcement (MAYBE-ADVERTISE-LOCAL-ADDRESS, protocol.lisp:2604-2637) — and that function's own docstring concedes the gap: Core's later self-announcements 'ride its outgoing addr queue, which we don't have'. That is an acknowledgement, not a reasoned deliberate divergence for the relay path, so it does not refute. CONSEQUENCE HOLDS: send time is welded to receive time, which is exactly the timing correlation the 30s exponential exists to break. One arithmetic nit in the filed impact — Core's flush count is bounded by the number of DISTINCT target peers (each address picks its own 2 by hash rank), not 2, so the true comparison is 'up to 20 immediate one-address messages' vs 'one batched message per distinct target per 30s window'; direction unchanged. Amplification is bounded elsewhere (relay only when the incoming message announced <=10 addrs, protocol.lisp:1337, plus the 0.1/s addr token bucket), so no DoS. Privacy hardening = S3.

### `0c63c99c` — S3

**CheckTxInputs (coinbase maturity, fee/value consensus) runs after the standardness-of-inputs, sigop and witness checks i**

CONFIRMED, executed. Core PreChecks (refs/bitcoin/src/validation.cpp): CheckTxInputs at :892 -> AreInputsStandard :896 -> IsWitnessStandard :901 -> GetTransactionSigOpCost :905, cap enforced :938. Ours (src/validation/transaction.lisp:1150-1200): are-inputs-standard-p :1163-1166 -> sigop cost + MAX_STANDARD_TX_SIGOPS_COST cap :1170-1173 -> is-witness-standard-p :1175-1181 -> validate-transaction-contextual (our CheckTxInputs) :1195. Both orderings claimed in the finding are real: CheckTxInputs is last, and the sigop cap precedes the witness check where Core's follows it. EXECUTED (dev.sh eval): two COINBASE utxos at height 100, spend at tip 100 (spend height 101, immature) -> (:IMMATURE+NONSTANDARD :NONSTANDARD-INPUTS :IMMATURE+STANDARD :COINBASE-NOT-MATURE :MATURE+NONSTANDARD :NONSTANDARD-INPUTS). So an immature coinbase spend whose prevout is bare-OP_TRUE reports "bad-txns-nonstandard-inputs" (TX_INPUTS_NOT_STANDARD) where Core reports "bad-txns-premature-spend-of-coinbase" (TX_CONSENSUS); with a standard prevout we report Core's reason, so the divergence needs both failures at once. The reject-cache consequence is real: src/networking/protocol.lisp:1466-1468 caches txid IN ADDITION to wtxid only for :nonstandard-inputs, so the swap changes which identifiers get cached -- but src/validation/block.lisp:2702-2708 clears recent-rejects on every tip change, bounding it to one block interval. NOT refuted, but no acceptance divergence: every one of the four checks is still present and each returns on first failure, so the accepted set is identical. The in-tree comment at :1155-1160 documents only the are-inputs-standard-before-sigop-cap ordering (which is correct vs Core); nothing documents CheckTxInputs-last as deliberate. S3 stands -- reject-reason vocabulary plus short-lived cache keying, no consensus or DoS reach. No Core functional test exercises the double-failure combination (mempool_spend_coinbase.py uses a standard coinbase output, which we get right).

### `0e7ec2f3` — S3

**/rest/mempool/contents.json ignores the ?verbose= and ?mempool_sequence= query parameters and always serves the verbose **

Mechanism confirmed by read + partial execution. src/rpc/rest.lisp:171-177 `%rest-mempool` is four lines: `(cond ((string= body "info") ...) ((string= body "contents") (%rest-json (rpc-getrawmempool node (list t)))) (t (%rest-error 400 ...)))` — verbose is the literal `t`, and neither "verbose" nor "mempool_sequence" appears anywhere in src/rpc/rest.lisp (grep for get-parameter in that file returns only lines 138, 389, 442 — headers `count`, blockpart `offset`/`size`). So the parameters are not merely mis-parsed, they are never read, and there is no 400 validation and no verbose+mempool_sequence conflict check. The query string IS available to the handler (rest-handle's docstring at :539 says "query already stripped by Hunchentoot" from script-name*, and %rest-size-parameter at :389 reads query params via hunchentoot:get-parameter), so this is an omission, not an architectural limit. Core side confirmed verbatim at refs/bitcoin/src/rest.cpp:796-821: value_or("true")/value_or("false"), two literal-string checks each returning HTTP_BAD_REQUEST naming the parameter, the `if (verbose && mempool_sequence)` 400 with the `hint: set "verbose=false"` text, then MempoolToJSON(pool, verbose, mempool_sequence). Consequence executed: on a warm image I added one tx to a test node's mempool and encoded both shapes — `(rpc-getrawmempool node (list t))` -> `{"80cc86df...":{"vsize":113,...}}` (JSON object keyed by txid, ~460 bytes for ONE tx) vs `(rpc-getrawmempool node (list nil))` -> `["80cc86df..."]` (JSON array, 68 bytes). So ?verbose=false does get a different JSON TYPE, not just a bigger one, exactly as claimed. Also confirmed: our getrawmempool ignores its own second (mempool_sequence) param (src/rpc/mempool.lisp:113-142 binds only `verbose`), so that snapshot is unreachable by either route. Direct `%rest-mempool` call aborts with "HUNCHENTOOT:*REPLY* is unbound" outside a request context, so the response-shaping half is read-verified rather than executed. One wording caveat that does not move severity: the "amplification the caller cannot opt out of" framing is soft — Core's REST default is verbose=true too, so an unauthenticated caller can pull the same verbose dump from Core; what is actually lost is the ability to ask for the cheap form. S3 stands (REST contract divergence, no consensus or DoS consequence).

### `105290bf` — S3

**Dotted section keys in bitcoin.conf (main.rpcport=..., test.connect=...) are treated as unknown options and dropped**

CONFIRMED, executed. Core: InterpretKey (refs/bitcoin/src/common/args.cpp:78-92) splits the key at the first '.' into section+name, and ReadConfigStream (common/config.cpp:93-115) runs it over EVERY config-file key before storing ro_config[key.section][key.name] — so `regtest.rpcport=3000` is a regtest-section setting. This is not an obscure internal: refs/bitcoin/doc/bitcoin-conf.md:44-46 documents it ("prefixed with a chain name; e.g., regtest.maxmempool=100") and gives a worked example where `regtest.rpcport=3000` (dotted) beats a later `[regtest] rpcport=4000`. Ours: src/config/conf.lisp:30-113 only recognises `[section]` headers; the key is handed whole to interpret-arg (src/config/args.lisp:19-56), which strips a `no` prefix but never a `section.` prefix. Grepped src/config/ for any '.'-splitting of keys — the only `(position #\.)` is conf-parse-money (values.lisp:114). Nothing handles it elsewhere. Executed Core's own doc example verbatim: (args->start-node-plist nil "regtest=1\nregtest.rpcport=3000\nrpcport=2000\n[regtest]\nmaxmempool=100") => :RPCPORT 2000 (Core gives 3000), merged carries ("regtest.rpcport" . "3000") untouched, and unknown-config-file-keys returns ("regtest.rpcport") => src/node/init.lisp:1566-1568 emits "Ignoring unknown configuration value regtest.rpcport". known-config-option-p "main.rpcport" => NIL. Consequence follows: Core's bitcoind reads the file with ignore_invalid_keys=true (common/init.cpp:38), so it applies these keys where we warn and drop them. Impact is correctly scoped in the finding (visible warning, not silent). Note the CLI half is NOT a divergence: Core rejects dotted keys on the command line outright (args.cpp:232-237 "Invalid parameter"), which matches our hard unknown-option refusal. S3 stands — config-only, one warning line per key, no consensus effect.

### `2279d91b` — S1

**coins-view-cache-sync clears DIRTY but leaves FRESH set, so coins already written to LevelDB are later dropped on spend **

Confirmed, and reproduced end-to-end in the warm image. Mechanism: src/storage/coins-view-cache.lisp:369-371 is the whole post-write pass — `(maphash (lambda (key ce) (declare (ignore key)) (setf (ce-dirty ce) nil)) (cvc-entries cache))` then `(setf (cvc-dirty-count cache) 0)`. ce-fresh is never touched, and spent tombstones stay in the table. The FRESH branch it feeds is :307-312 (remhash, no erase staged). Core's Sync (refs/bitcoin/src/coins.cpp:291-300) builds its cursor with will_erase=false; NextAndMaybeErase (coins.h:279-295) erases spent entries from the map and otherwise calls SetClean(), which is `m_flags = 0` (coins.h:175-182) — BOTH flags — and Sync then throws std::logic_error("Not all unspent flagged entries were cleared") if any survive. coins.h:150-159 states the invariant verbatim: marking a coin FRESH when it exists unspent in the parent "will cause a consensus failure". Reachability confirmed: coins-view-cache-sync has exactly one caller, %coin-view-iterate (:896), reached from utxo-set-iterate / compute-utxo-set-hash / -muhash / -total-amount / -distinct-txids, i.e. gettxoutsetinfo (src/rpc/blockchain.lisp:1784-1805), dumptxoutset (:1170), scantxoutset (:1535) and the assumeutxo hash check (src/node/assumeutxo.lisp:290) — all on `(rpc-get-utxo-set node)`, which src/rpc/accessors.lisp:43-47 returns as the LIVE `chain-state-coins-view` of the current chainstate, not a copy. And coin-view-apply-block (:564-572) adds non-coinbase outputs with `:allow-overwrite is-coinbase` = NIL, so every one of them is fresh=T. EXECUTED reproduction (fresh tmp LevelDB + coins-view-cache, one non-coinbase coin added exactly as apply-block does, then utxo-set-iterate to trigger the sync): ((:AFTER-ADD :FRESH T :DIRTY T) (:AFTER-SYNC :FRESH T :DIRTY NIL :IN-BASE T) (:SPEND-RETURNED T) (:AFTER-SPEND :SLOT-PRESENT NIL :CACHE-HAS-P T :DIRTY-COUNT 0) (:SECOND-SPEND-RETURNED T) (:BASE-AFTER-FLUSH NIL)). Read that middle line: after the spend the cache slot is GONE, no erase is staged (dirty-count 0), and coin-view-has-p answers T again — the coin is unspent once more, re-fetched from LevelDB. The second spend of the same outpoint therefore succeeds, which is the consensus split. Re-running without the second spend gives (:BASE-AFTER-FLUSH T): the coin survives in the coins DB as unspent forever, so the leak also persists across a restart and corrupts hash_serialized_3 / any assumeutxo snapshot taken afterwards. Window is large, not incidental: maybe-periodic-flush (src/node/flush.lisp:315-350) only does a real flush every N blocks / N seconds / at the dbcache siz

### `24e0c216` — S3

**/rest/block/<hash>.json renders getblock verbosity 2; Core renders verbosity 3 (SHOW_DETAILS_AND_PREVOUT)**

CONFIRMED, executed. Ours: src/rpc/rest.lisp:106 `(rpc-getblock node (list body (if notxdetails 1 2)))` — 2 for the plain endpoint. src/rpc/blockchain.lisp:274 gates prevouts as `:prevouts (>= verbosity 3)`, and tx-to-json (blockchain.lisp:450-476) documents/implements fee-at-2-and-3 vs prevout-only-at-3; an existing test (tests/rpc/rpc-tests.lisp:3333-3360) asserts verbosity 2 must NOT carry prevout. Core: rest.cpp:470-473 rest_block_extended passes TxVerbosity::SHOW_DETAILS_AND_PREVOUT and rest.cpp:1146 registers it for "/rest/block/" (notxdetails -> SHOW_TXID); blockchain.cpp:867-874 reaches that level only at getblock verbosity 3; blockToJSON blockchain.cpp:223-238 renders the prevout via TxToUniv when undo is present.
EXECUTED: built a block (coinbase + one spend) in a temp block store, seeded its undo entry, then compared on the same node ->
  :REST-VIN1-KEYS  (txid vout scriptSig sequence)
  :V2-VIN1-KEYS    (txid vout scriptSig sequence)
  :V3-VIN1-KEYS    (txid vout scriptSig prevout sequence)
So /rest/block/<hash>.json is byte-for-byte our verbosity 2 and omits "prevout", while our verbosity 3 emits it — the capability exists and is simply not requested. No comment in rest.lisp justifies the 2 (the file header's "NOT supported" list is about other endpoints). Consequence follows but is a rendering-completeness gap for explorer/fee clients only: S3 unchanged.

### `272e30f0` — S3

**CONST_SCRIPTCODE's OP_CODESEPARATOR rule is applied only to the scriptPubKey and to a scriptCode reached by a CHECKSIG, **

Core side verified at refs/bitcoin/src/script/interpreter.cpp:474-476: the check sits in EvalScript's opcode loop ABOVE the `if (fExec && ...)` guard, so it fires in unexecuted branches, and EvalScript runs on scriptSig, scriptPubKey and the P2SH redeem script. Our side: the only two CONST_SCRIPTCODE codesep scans in the whole tree are verify-script Step 0a (src/coalton/interop.lisp:804-818, scriptPubKey only) and verify-checksig (interop.lisp:2097-2112, `(or *current-script-code* script-pubkey)`, only when a CHECKSIG executes). `grep -rn 'CONST_SCRIPTCODE|op-codeseparator|#xab' src/` confirms nothing else — the check has not moved. EXECUTED with *script-flags* = "P2SH,CONST_SCRIPTCODE": P2SH spend of redeemScript `ab 51` (OP_CODESEPARATOR OP_1) => (T NIL) accepted; scriptSig `ab 51` against scriptPubKey `51` => (T NIL) accepted; scriptPubKey `ab 51` => (NIL :OP-CODESEPARATOR), i.e. only the scriptPubKey arm matches Core. One refinement to the write-up: the scriptSig arm is NOT reachable for relay, because a scriptSig containing OP_CODESEPARATOR is not push-only and our own standardness rejects it (scriptsig-push-only-p, src/validation/transaction.lisp:410), exactly as Core's IsStandardTx does. The redeemScript arm IS reachable — it passes Core's AreInputsStandard (push-only scriptSig, 0 sigops) and then fails Core's PolicyScriptChecks with SCRIPT_ERR_OP_CODESEPARATOR while we accept and relay. Relay-only: CONST_SCRIPTCODE is in +standard-policy-flags+ (src/validation/block.lisp:264) and never in the block flag set, so S3 stands.

### `2834e5b3` — S3

**JSON-RPC batch members never get the named-argument transform, so any batch call using named params fails with -32603**

Confirmed by executing both paths side by side in the warm image. handle-batch-request (src/rpc/server.lisp:411-431) binds params as `(%normalize-rpc-params (or (gethash "params" req) '()))` with no %named-params-to-positional, while the singleton path (src/rpc/server.lisp:303-305) wraps the same value in %named-params-to-positional first. %normalize-rpc-params leaves a hash-table a hash-table (src/rpc/server.lisp:143-147 falls through to %normalize-json-value, which normalizes hash tables in place). Executed: for the request {jsonrpc:2.0, id:1, method:getblockhash, params:{height:0}}, parse-json-rpc-request returns (:SINGLE "getblockhash" (0)) — transformed to positional — whereas handle-batch-request on the identical member returns {"jsonrpc":"2.0","error":{"code":-32603,"message":"Internal error: The value #<HASH-TABLE :TEST EQUAL :COUNT 1 ...> is not of type LIST"},"id":1}. That is exactly the claimed -32603 with a Lisp type error in the message. Core's side checks out: batch members run through JSONRPCExec -> CRPCTable::execute -> ExecuteCommands -> ExecuteCommand, and ExecuteCommand (rpc/server.cpp:502-512) applies transformNamedArguments whenever request.params.isObject(), so named parameters behave identically in a batch and a singleton there. Severity S3 is right — it is a client-facing usability/compat defect with a misleading error, not a security or consensus issue.

### `2f0cf648` — S2

**Outbound netgroup-diversity set is built from ALL peers including inbound, so free inbound connections can veto our outb**

CONFIRMED, and reproduced by execution. src/node/peers.lisp:647-652 builds used-addrs as (mapcar #'bl.net:peer-address (node-peers node)) with no conn-type/inbound filter, and :665-668 skips any candidate whose ip-netgroup is in used-groups. node-peers does contain inbound peers: merge-inbound-peers (src/node/eviction.lisp:9-44) pushes handshaked inbound peers onto node-peers, and count-inbound-peers counts them there. Contrast the function's own neighbour count-outbound-full-relay-peers (peers.lisp:500-504), whose docstring explicitly excludes inbound 'so an attacker filling our inbound slots cannot suppress replacement outbound dials' -- the arithmetic was fixed, the netgroup set was not. Core net.cpp:2657-2687 switches on m_conn_type and breaks (contributes no group) for INBOUND/ADDR_FETCH/FEELER/PRIVATE_BROADCAST with the comment 'Since they are free to make, an attacker could make them to prevent us from connecting to certain peers', counts Tor/I2P/CJDNS separately as outbound_privacy_network_peers, and only then rejects at net.cpp:2825-2827 -- core_ref accurate.

EXECUTED (scripts/dev.sh eval, synthetic node, single candidate 203.0.113.9:8333 in the address book so the dial is observable via peer-address-last-attempt):
  (:CANDIDATE-GROUP "203.0" :DIALED-WHEN-INBOUND-SHARES-16 NIL :DIALED-WHEN-INBOUND-OTHER-16 T :DIALED-WHEN-NO-PEERS T)
One inbound peer at 203.0.113.44 -- costing an attacker nothing but a TCP connect -- vetoes the replacement dial outright; an inbound peer in another /16 does not. Exactly Core's documented primitive.

Consequence also holds: node-known-addresses is written in exactly one place (peers.lisp:411, inside connect-to-peers) and read at :416 and :657 -- there is no other writer, and connect-to-peers only runs at startup (init.lisp:892) and from %sync-offline-activation (init.lisp:1117), which is reached only when (length (node-peers node)) is 0. With inbound peers present that branch never runs, so replace-disconnected-peers is the sole refill path for the outbound full-relay pool and works from a frozen candidate list whose /16 groups an attacker can enumerate and cover.

Two small inaccuracies in the finding's 'we do' text that do not change the verdict: feeler peers are never in node-peers (do-feeler-connection disconnects without pushing, peers.lisp:905-936), and including BLOCK_RELAY peers matches Core (Core's BLOCK_RELAY case does insert a group). The real divergences are inbound, addr-fetch (-seednode peers are pushed via establish-outbound-peer, peers.lisp:818), and the missing onion/i2p/cjdns carve-out. Severity 

### `32758a48` — S3

**A negative -maxconnections is accepted and clamped instead of being an init error**

CONFIRMED mechanism, executed; one clause of the stated impact does NOT follow. Core init.cpp:1031-1035: user_max_connection < 0 => InitError("-maxconnections must be greater or equal than zero"). Ours: src/config-options.lisp:50 is `(define-option "maxconnections" :key :max-connections :type :int)` with no :min, and the :min facility exists and is used elsewhere (bytespersigop, maxtipage, peertimeout... registry.lisp:59-85), so its absence here is the gap. Executed: (args->start-node-plist '("-maxconnections=-1")) => :MAX-CONNECTIONS -1, :LISTEN NIL, no error; (automatic-inbound-capacity -1 8) => 0 (src/node/peers.lisp:861-871 clamps with (max 0 ...)), and conf-effective-listen-flags '(("maxconnections" . "-1")) => NIL (values.lisp:322-327), with apply-parameter-interactions (src/node/args.lisp:158-164) also soft-setting *dns-seed-enabled*=NIL. So the node starts with inbound capacity 0, listening off and DNS seeding off where Core refuses to start — the divergence and its main consequence hold. CORRECTION to the finding's last sentence: "The node looks up but never gains peers" is wrong. :max-peers is NOT a config option (grep of src/config-options.lisp finds no max-peers row), so it stays at start-node's default 8 and outbound dialling is unaffected by -maxconnections — this tree deliberately does not clamp outbound to the total (documented at peers.lisp:866-871), and fixed seeds are still enabled, so the node still acquires outbound peers. The real cost is silent loss of inbound/listen/DNS-seed instead of an immediate refusal. Severity already at the floor: S3 unchanged.

### `34a807d1` — S3

**ECDSA satisfaction sized at 72 bytes on a stated justification that is factually false — our signer does grind low-R**

CONFIRMED, executed. The header comment at src/wallet/wallet-spend.lisp:392-397 and divergence 1 at :48-52 justify +ecdsa-max-sig-size+ = 72 (:399) with "our signer produces RFC6979 signatures WITHOUT low-R grinding". That premise is false: src/crypto/secp256k1.lisp:309-352 is Core's CKey::Sign grind loop (increments a 4-byte LE extra-entropy counter until compact[0] < 0x80) and its own docstring says "Byte-identical to Bitcoin Core's CKey::Sign (grind=true)". Executed in the warm image: 200 signatures over distinct hashes returned DER length 70 for all 200 ((70 . 200)) — i.e. 71 bytes incl. the sighash byte, never 72. The wallet spend and PSBT signers both reach that function (src/rpc/rawtransaction.lisp:407,413 legacy-sig/bip143-sig, used by %compute-input-signatures, which src/wallet/psbt.lisp:1254 and the spend signer call). Core is described correctly: spend.cpp:54-58 UseMaxSig is only external-selected inputs, MaxInputWeight (:70-72) passes !can_grind_r || UseMaxSig, and wallet.cpp:4119-4121 CanGrindR() == !DISABLE_PRIVATE_KEYS, so a private-key descriptor wallet gets 71 (descriptor.cpp use_max_sig ? 72 : 71). Consequence also holds: wallet-spend.lisp:2184-2215 pins current-fee to fee-needed = effective-feerate x estimated vsize and only GROWS change when current-fee exceeds it; nothing after signing (:2269-2305) refunds the difference, exactly as Core. So we pay ~1 vB/ECDSA-input more than Core (legacy 4 WU, segwit 1 WU) and our effective values differ slightly in coin selection. Severity stays S3: the error is always in the safe direction (never underpays), bounded by 1 vB x feerate per input, and we still match Core for watch-only/externally-selected inputs where Core itself uses 72. What is unambiguously wrong is the stated reason.

### `38bb5cc7` — S3

**CheckTxInputs' input-value MoneyRange guards and ConnectBlock's accumulated-fee guard have no analogue**

MECHANISM CONFIRMED, executed; the finding's own impact statement already says it is defence-in-depth, and one of its two named ingress paths turns out to be guarded. Read: grep for +max-money+/MoneyRange over src/ finds range checks ONLY on OUTPUT values (src/validation/transaction.lisp:83 per-output, :89 total-output — Core's bad-txns-vout-toolarge / txouttotal-toolarge), on RPC amounts, on a fee-rate relay cap, and in the assumeutxo loader. validate-transaction-contextual (transaction.lisp:119-190) sums input values with satoshi+ and never range-checks a coin value, nValueIn, or the derived fee; %contextual-check-block accumulates total-fees at block.lisp:~1777 and feeds it straight into max-coinbase-value with no range check. Core side verified verbatim: refs/bitcoin/src/consensus/tx_verify.cpp:184-187 (bad-txns-inputvalues-outofrange), :202-209 (bad-txns-fee-outofrange), refs/bitcoin/src/validation.cpp:2574-2578 (bad-txns-accumulated-fee-outofrange). Satoshi is (Satoshi Integer), src/coalton/types.lisp:74-77 — unbounded, so no overflow, as stated.

EXECUTED PROOF (scripts/dev.sh eval): handed validate-transaction-contextual a coin worth 3x MAX_MONEY through the pending-utxos overlay:
  (:MAX-MONEY 2100000000000000 :COIN-VALUE 6300000000000000 :OK T :ERR NIL :FEE 6299999999999999 :FEE-IN-RANGE NIL)
Accepted, with an out-of-range fee. Core rejects bad-txns-inputvalues-outofrange.

CONSEQUENCE CORRECTION: the finding names "a UTXO snapshot" as a live ingress; that is wrong — the assumeutxo loader DOES range-check every coin (src/rpc/blockchain.lisp:1279-1284: `(when (or (minusp value) (> value bl.val:+max-money+)) ... "bad tx out value")`, Core's own snapshot check). The only remaining ingress is a corrupted chainstate record, which is not adversarially reachable; on any chain built from genesis the supply bound is enforced inductively by the output-side checks, so neither the per-coin nor the accumulated-fee guard can fire. Genuine missing hardening, no live bug. Severity S3 stands.

### `3911beba` — S3

**-bind / -whitebind together with an explicit -listen=0 is accepted; Core makes it a hard init error**

Read + EXECUTED. conf-effective-listen-flags (src/config/values.lisp:313-336) replays Core's soft-set chain -- explicit -listen is soft-set first at :319-321 so it wins over the -bind soft-set at :321-322 -- and its ONLY config-error is the -listen=0/-listenonion=1 pair at :333-334. Searched for a moved check: grep -rni "whitebind" over src/ and grep -rni "together with -listen" over src/ turn up no -bind/-listen=0 refusal anywhere in src/config/, src/node/ or src/networking/. Executed: (args->start-node-plist '("-regtest" "-bind=127.0.0.1" "-listen=0")) returns (:LISTEN-ONION NIL :WALLET-NAMES NIL :LISTEN-BIND "127.0.0.1" :LISTEN NIL :NETWORK :REGTEST) with no error, and (args->start-node-plist '("-regtest" "-whitebind=noban@127.0.0.1:19444" "-listen=0")) returns (... :WHITEBIND ("noban@127.0.0.1:19444") :LISTEN NIL ...) with no error. Core refs/bitcoin/src/init.cpp:1016-1020 (verified by sed) sums GetArgs("-bind")+GetArgs("-whitebind") and returns InitError("Cannot set -bind or -whitebind together with -listen=0") -- a hard refusal, immediately above the listenonion check we DO implement. Divergence and consequence both real: the node starts with a recorded bind address it never listens on, silently discarding the operator's explicit binding. No security exposure follows -- the whitebind permissions (bl.net::*whitebind-flags*, src/networking/netaddress.lisp:724-742) apply to inbound peers only, and with :listen NIL none can arrive -- so this stays a startup-hygiene divergence. S3 unchanged.

### `3f90b8bc` — S3

**getmininginfo reports currentblockweight/currentblocktx as 0 before any template has been assembled; Core omits the fiel**

CONFIRMED, EXECUTED. /Users/sen/common-lisp/bitcoin-lisp/src/rpc/mining.lisp:456-457 builds the alist unconditionally:
  ("currentblockweight" . ,(if template (bl.mining:block-template-total-weight template) 0))
  ("currentblocktx"     . ,(if template (length (bl.mining:block-template-transactions template)) 0))
with `template` = bl.mining:*last-block-template* (:443). Ran rpc-getmininginfo on a fresh node with *last-block-template* bound to NIL:
  (:LAST-TEMPLATE NIL :HAS-WEIGHT ("currentblockweight" . 0) :HAS-TX ("currentblocktx" . 0))
Core omits both: refs/bitcoin/src/rpc/mining.cpp:466-467 `if (BlockAssembler::m_last_block_weight) obj.pushKV(...)`, the fields being `std::optional<int64_t>` at node/miner.h:95-99. mining_basic.py:388-389 asserts `'currentblocktx' not in mining_info` / `'currentblockweight' not in mining_info` after generate-only mining, which our reply fails.

Mechanism confirmed; consequence is exactly what S3 describes, so severity unchanged. One half of the filed impact is weaker than stated: 0 is unreachable as a real weight for US too (bl.mining::block-template total-weight is initialised to *block-reserved-weight*, src/mining/assembler.lisp:~78), so the weight sentinel is in practice unambiguous. The genuine ambiguity is only currentblocktx, where an empty template legitimately reports 0. No consensus, security or node-behaviour consequence — a JSON key-shape divergence plus one functional-test assertion.

### `42dacfaf` — S2

**Every dial counts an addrman failure — Core suppresses failure counting when the node looks offline, and never counts it**

CONFIRMED, executed. src/node/peers.lisp:206-231 %record-dial-attempt hard-codes `:count-failure t` with no online/offline test and no proxy carve-out, and it is called BEFORE the dial from replace-disconnected-peers (:672), establish-outbound-peer (:784) and do-feeler-connection (:923); %record-outbound-result (:297) likewise passes `:count-failure t` on its failure branch. establish-outbound-peer is the single entry for addr-fetch/-seednode (:817), -connect (:827/:840), addnode (:854), the addconnection RPC (:849) and block-relay slots (:910), so all of them count failures. Core: ConnectNode calls `addrman.Attempt(target_addr, fCountFailure)` only `if (!proxyConnectionFailed)` (net.cpp:492-497); ThreadOpenConnections computes `count_failures{((int)outbound_ipv46_peer_netgroups.size() + outbound_privacy_network_peers) >= std::min(m_max_automatic_connections - 1, 2)}` with the comment "Don't record addrman failure attempts when node is offline" (net.cpp:2884-2891); and I verified every other OpenNetworkConnection call site passes literal false — 1905 (addconnection), 2422 (ADDR_FETCH), 2541 (-connect MANUAL), 2986 (added nodes). Only the PRIVATE_BROADCAST site (3253) passes true, a feature we do not have. Grepped for any offline gate in src/: `count-failure` appears only at addrman.lisp:480/485 and peers.lisp:231/297 — none is conditional.

Downstream chain executed against our own addrman: address-book-attempt does implement Core's m_last_good epoch guard (addrman.lisp:485-488), so damage is +1 per address per good-epoch, as the finding itself says. Simulating four epochs on one never-successful address gave n-attempts 1,2,3,4 with addr-info-terrible-p flipping to T at 3 (+addrman-retries+) and addr-info-chance falling 0.66 -> 0.436 -> 0.287 -> 0.190. Terrible entries are then skipped by GetAddr (addrman.lisp:571) and become overwrite targets in address-book-add (:444) — both consumers exist and match Core, so the stated consequence follows. Reachable: maintain-peers (peers.lisp:1046) calls replace-disconnected-peers every cycle, and its loop walks the whole known-address list when nothing connects. Caveats worth recording: the harm needs several outages separated by at least one success (m_last_good must advance), it touches only addresses actually dialed rather than literally "the whole table", and Core itself starts counting once 2 outbound netgroups exist. Mechanism exact, consequence real but gradual; S2 kept as an eclipse-resistance/addrman-hygiene regression, not a crash.

### `5054e381` — S3

**wtxidrelay / sendaddrv2 received after VERACK are silently ignored where Core disconnects**

MECHANISM CONFIRMED BY EXECUTION. Core WTXIDRELAY net_processing.cpp:3927-3933 and SENDADDRV2 :3949-3955 both open with `if (pfrom.fSuccessfullyConnected) { LogDebug(... "received after verack"); pfrom.fDisconnect = true; return; }`. Ours, src/networking/protocol.lisp:257-263, returns T with the comment 'No-op post-handshake (only meaningful during handshake)'. Grepped for a disconnect that MOVED: `grep -rn 'after verack|post-verack|post-handshake' src/networking/` finds the disconnect only for sendtxrcncl (:266-274); the handshake-window handlers that DO set the flags live in peer.lisp:1132/1134 inside %AWAIT-VERACK and are unreachable post-verack. Executed against a :READY peer: handle-message 'sendaddrv2' and 'wtxidrelay' both return T with (:STATE :READY :WANTS-ADDRV2 NIL :WTXID-RELAY NIL) — so the filed impact's own caveat is right, no relay state flips, and this stays hardening rather than a live relay bug. TWO CORRECTIONS to the finding's text, neither fatal. (1) The parity test is misnamed: refs/bitcoin/test/functional/p2p_handshake.py contains no such assertion; the asserting test is p2p_addrv2_relay.py:79-83, `with self.nodes[0].assert_debug_log(['sendaddrv2 received after verack, disconnecting peer=0']): conn.send_without_ping(msg_sendaddrv2()); conn.wait_for_disconnect()` — a concrete failing oracle case, which strengthens rather than weakens the finding. (2) The claim that our sendtxrcncl comment misreads Core is arguable: 'Core disconnects ..., unlike the sendaddrv2/wtxidrelay no-op stubs above' reads at least as naturally as contrasting Core's behaviour with OUR stubs as with claiming Core no-ops them. Either way no comment documents this as a deliberate divergence. S3 stands.

### `508fedab` — S3

**PSBT parser omits two of Core's global-map validity checks (out-of-range prevout index, unsupported PSBT version)**

MECHANISM CONFIRMED, executed; STATED IMPACT DOES NOT FOLLOW. I hand-built a PSBT whose global map declares version 2 and whose single input carries a non_witness_utxo with the matching txid but a prevout index of 5 against a prev tx with 1 output, plus a witness_utxo of value 1 (real output value 100000). parse-psbt accepted it: (:PARSE-RESULT (:ACCEPTED :DECLARED-VERSION #(2 0 0 0) :RESOLVED-PREVOUT-VALUE 1)). Core rejects both: psbt.h:1322-1323 `if (*m_version > PSBT_HIGHEST_VERSION) throw ...("Unsupported version number")` with PSBT_HIGHEST_VERSION = 0 (psbt.h:80), and psbt.h:1375-1377 `if (tx->vin[i].prevout.n >= input.non_witness_utxo->vout.size()) throw ...("Input specifies output index that does not exist")`. Our parse-psbt (src/serialization/psbt.lisp:203-232) only length-checks the version record and %psbt-validate-input (psbt.lisp:178-201) only checks deserialization, trailing bytes and the txid — no bounds check anywhere; grep over src/ finds the check has not MOVED. The fallback the finding predicts is real and I triggered it: %psbt-input-prevout (src/wallet/psbt.lisp:197-243) bounds-checks and then falls through to the unauthenticated witness_utxo, returning value 1. But the stated CONSEQUENCE — reopening the fabricated-fee theft the comment at wallet/psbt.lisp:204-222 closes — is not reachable. Triggering the fallback requires prevout.n to exceed the real prev tx's vout count, i.e. an outpoint that does not exist on chain, so the transaction the victim signs can never confirm and "the difference goes to the miner" cannot happen; a legacy sighash also commits to the outpoint, so the signature is not reusable. I also checked every other non_witness_utxo consumer (wallet/psbt.lisp:400, 630, 670, 1028-1049, 1105, 1600; wallet-spend.lisp:2957) for an unguarded aref — there is none, so no crash path either. What survives is exactly an S3: we decode PSBTs Core refuses, and decodepsbt/analyzepsbt report an attacker-chosen input amount on an unmineable PSBT. S3 is the right bucket; the impact text should be corrected.

### `54fdda5c` — S3

**Duplicate-in-mempool check is txid-only: Core's "txn-same-nonwitness-data-in-mempool" case is never reported**

CONFIRMED, executed. src/validation/transaction.lisp:1085-1089 is a single (bl.mp:mempool-has mempool txid) probe returning :already-in-mempool; the reason table at :905 maps it to "txn-already-in-mempool" and grep over src/ finds no "same-nonwitness" keyword or string anywhere. Core validation.cpp:823-830 probes exists(GetWitnessHash()) first, then exists(GetHash()) -> "txn-same-nonwitness-data-in-mempool". EXECUTED: built two txs with identical non-witness data and different witnesses, added A to the mempool, validated B -> (:SAME-TXID T :DIFF-WTXID T :B-WTXID-IN-POOL NIL :REASON :ALREADY-IN-MEMPOOL :REASON-STRING "txn-already-in-mempool"). B's wtxid is genuinely absent from mempool-by-wtxid, so Core would take its second branch. Consequence is real and named: refs/bitcoin/test/functional/mempool_accept_wtxid.py:66-72 asserts testmempoolaccept reports exactly "txn-same-nonwitness-data-in-mempool" for child_two -- that assertion fails against us. NOT a defect elsewhere-handled: the PACKAGE path does distinguish it (src/validation/packages.lisp:615-620 sets :different-witness with other-wtxid, surfaced by src/rpc/mempool.lisp:740), matching Core's MempoolAcceptResult::DIFFERENT_WITNESS, so the gap is confined to the single-tx path -- which is exactly what the finding says. No acceptance divergence (txid presence is a strict superset of wtxid presence; both branches reject with TX_CONFLICT and neither punishes the peer, per p2p_orphan_handling.py:574), and sendrawtransaction quits early and relays the pooled wtxid in both trees (src/rpc/mempool.lisp:662-669). S3 stands.

### `56b53a04` — S2

**getnetworkhashps ignores its `height` argument entirely, does not implement nblocks = -1, and never validates its inputs**

CONFIRMED, EXECUTED. /Users/sen/common-lisp/bitcoin-lisp/src/rpc/mining.lisp:799-820 reads only `(first params)`: `(let ((n (first params))) (if (and (integerp n) (> n 0)) n 120))`, then always anchors at `(bl.store:best-block-hash chain-state)`. `(second params)` never appears in the body. No validation lives elsewhere: the only other mentions of the method are src/rpc/core-tables.lisp:59 and :269, which are the JSON-arg-conversion / arg-name tables (data for the HTTP layer), not range checks.

Ran it. Built an 11-entry synthetic index (heights 0..10, 600 s apart, 1e6 chain-work/block) on a make-test-node and called bl.rpc::rpc-getnetworkhashps directly:
  (:HEIGHT 10 :DEFAULT 1667 :N5 1667 :N5-H0 1667 :N5-H3 1667 :N5-H999 1667 :MINUS1 1667 :ZERO 1667 :STR 1667)
Every variant returns the same tip-window number and none signals. Core (refs/bitcoin/src/rpc/mining.cpp:65-89): `lookup < -1 || lookup == 0` -> RPC_INVALID_PARAMETER "Invalid nblocks..."; `height < -1 || height > active_chain.Height()` -> "Block does not exist at specified height" (so h=999 on a height-10 chain is an error, we answer 1667); `height >= 0` re-anchors pb at active_chain[height] and returns 0 when `!pb->nHeight` (so h=0 is 0, we answer 1667); `lookup == -1` expands to `pb->nHeight % DifficultyAdjustmentInterval() + 1`.

Mechanism and consequence both hold. Upgrading to S2: this is not a cosmetic divergence — a documented argument is silently discarded and the RPC answers a plausible-looking number about the wrong block, and three of Core's error paths (-8) return a value instead. rpc_blockchain.py:496-547 (`DIFFICULTY_ADJUSTMENT_INTERVAL = 144` at :69, the -1 expansion at :541) exercises exactly these.

One detail in the filing is understated rather than wrong: our window uses the tip and start-block timestamps, where Core uses min/max over the window (mining.cpp:91-100) — that is the separate finding at docs/gap-analysis-10.md:482, not this one.

### `57e206a8` — S3

**The BIP94 timewarp floor and check use the fixed 2016-block interval and a hardcoded testnet4 gate; the comment justifyi**

CONFIRMED, EXECUTED, and the "documented deliberate divergence" escape does not apply — the comment's justification is factually wrong.

Both halves of the mechanism verified by running the two functions. With tip time T=2000000000, MTP=T-3600, and a header at T-5000:
  (:MINTIME-144 1999996401 :MINTIME-2016 1999999400 :CORE-REGTEST-WOULD-BE 1999999400
   :VIOLATION-REGTEST-H144 NIL :VIOLATION-TESTNET4-H144 NIL :VIOLATION-TESTNET4-H2016 T)
So at a regtest retarget boundary (height 144) bl.mining:next-block-mintime returns MTP+1, while Core's GetMinimumTime(pindexPrev, 144) returns prev.time - MAX_TIMEWARP — 2999 s apart, and that value is what getblocktemplate reports as `mintime`. Core's floor is genuinely unconditional on enforce_BIP94 (refs/bitcoin/src/node/miner.cpp:36-47, comment "Account for BIP94 timewarp rule on all networks"), and the interval is a parameter, `nPowTargetTimespan / nPowTargetSpacing` (consensus/params.h:126), = 86400/600 = 144 on regtest (kernel/chainparams.cpp:576-577). Ours is the global constant 2016: src/storage/chain.lisp:424 `(defconstant +difficulty-adjustment-interval+ 2016)` — evaluated in-image as 2016, +max-timewarp+ = 600 — used at src/mining/assembler.lisp:90 and src/validation/block.lisp:639, with no per-network variant anywhere (grep over src/ finds 6 call sites, all on that one constant). Main/test/testnet4/signet all use 2016, so regtest is the only network that diverges.

The consensus half also holds: bip94-timewarp-violation-p is gated `(eq bl:*network* :testnet4)` (block.lisp:637) and returns NIL on :regtest even for a header 5000 s before its predecessor at height 144, and we cannot be made to enforce it — `-test` sits in define-core-only-options (accepted, not implemented) at src/config-options.lisp:407, so `-test=bip94` (chainparams.cpp:47 `HasTestOption(args, "bip94")`, kernel/chainparams.cpp:579) is silently a no-op for us. mining_basic.py:193-247 drives exactly this with DIFFICULTY_ADJUSTMENT_INTERVAL = 144 (:53), asserting `tmpl['mintime'] == tmpl['curtime']` and a -25 'time-timewarp-attack' from submitheader.

The assembler comment at :245-256 ("matches every network except regtest (Core: 144), where BIP94 is off and the extra floor is moot") is wrong on the template half — Core applies the floor on regtest regardless of BIP94, proven by the 2999 s gap above — and only conditionally right on the consensus half, since regtest CAN enforce BIP94. So this is a divergence documented with incorrect reasoning, not a reasoned one.

Severity unchanged at S3: the whole blast radius is regtest. testnet4's interv

### `606ac3e5` — refuted

**address-book-add is missing Core's "do not update if no new information is present" gate and its nTime refresh interval**

REFUTED on consequence (the code reading is right; the impact is not reachable, and the finding misreads what Core's gate actually blocks). Mechanism half is true: addrman.lisp:413-421 refreshes last-seen on any strictly newer penalized time with no 1h/24h update_interval, and there is no equivalent of Core's `if (addr.nTime <= pinfo->nTime) return false;` (addrman.cpp:576-579) before the in-tried / refcount-8 / `1 << nRefCount` checks.

But the claimed attack — "a peer replaying an identical addr record keeps getting fresh rolls ... Core refuses the roll outright" — does not hold. Core's gate compares the UNPENALIZED addr.nTime against the STORED (penalized) pinfo->nTime. Relayed ADDR records carry time_penalty=2h (net_processing Add(vAddrOk, pfrom.addr, 2h); our equivalent is +addr-gossip-time-penalty-seconds+ at protocol.lisp:1245-1251), so after the first insert pinfo->nTime = T-2h and a replay of the same record evaluates `T <= T-2h` = false: Core falls through to the very same stochastic roll we do. The gate only fires for time_penalty=0, which Core and we both reserve for a self-announcement (addr == source), i.e. exactly the case where the source netgroup is fixed — and even there an attacker sidesteps it for free by incrementing the timestamp, which passes Core's gate too.

Executed: replaying an identical record 40x through address-book-add left ref-count at 1 for both penalty 0 and penalty 7200 (the new-bucket index is keyed by source group, so the same source always lands on the same slot and `(/= cur id)` is false). Positive control: the same address added 60x from 12x7 distinct source groups reached ref-count 5 — so multiplication requires many distinct source netgroups, which our missing gate does not affect, since at penalty 2h Core permits it identically. The "up to the 8-bucket maximum without new information" impact is therefore unreachable. The second impact also fails: Core's throttle still refreshes nTime hourly for a currently-online address, which keeps an entry equally far inside the 30-day ADDRMAN_HORIZON — the interval is a churn/granularity nicety, not a staleness guarantee. Residual: a real but consequence-free divergence (extra bucket-lookup work and finer-grained nTime tracking on penalty-0 paths). Left at S3 as the schema has no lower rank, but it should be treated as a cosmetic parity gap, not a security finding.

### `631b90f9` — S3

**-forcednsseed=1 with -dnsseed=0 (or with -connect) is silently ignored instead of being an init error**

CONFIRMED, executed. Core init.cpp:1009-1013 (inside AppInitParameterInteraction) returns InitError("Cannot set -forcednsseed to true when setting -dnsseed to false."), and bitcoind.cpp:187-192 runs InitParameterInteraction FIRST, so the soft-set of -dnsseed=0 from -connect / -maxconnections<=0 (init.cpp:777-784) and from a clearnet-free -onlynet (init.cpp:835-843) makes the check fire for `-forcednsseed -connect=x` too. Ours: grep -rn forcednsseed over src/ finds exactly three sites — the defvar (src/config.lisp:92), the option row (src/config-options.lisp:338, plain :global *force-dns-seed*), and the single consumer (src/node/peers.lisp:352, `(and *force-dns-seed* *dns-seed-enabled*)`). No consistency check anywhere: the only config-errors in src/node/args.lisp are the -onlynet ones (:219-245) and src/node/init.lisp's are prune/txindex/peerblockfilters/mocktime etc.; none mention forcednsseed. Executed: (apply-config-globals '(("forcednsseed" . "1") ("connect" . "1.2.3.4"))) returns normally with *FORCE-DNS-SEED*=T and *DNS-SEED-ENABLED*=NIL — node starts where Core refuses. tests/config/config-tests.lisp:1469-1481 pins this as current behaviour, but its comment reasons only about PRECEDENCE ("-forcednsseed does not override -dnsseed=0"), which Core shares; it does not document the missing InitError as a deliberate divergence, so it is an omission, not a reasoned deviation. One mild impact correction: the operator is not left with nothing — src/node/init.lisp:1190-1192 logs "DNS seeding disabled (-dnsseed=0)" at startup, so the state is visible in the log even though the contradiction is never named and startup is not refused. S3 stands.

### `6635e4c9` — S3

**Package consistency check adds inputs one at a time, misreporting a transaction with duplicate inputs as a package confl**

CONFIRMED, executed, and Core has a unit test pinning the opposite behaviour. src/validation/packages.lisp:128-138 does (when (gethash key spent) ... :conflict-in-package) and (setf (gethash key spent) t) in the SAME per-input iteration, and has no empty-vin guard. Core policy/packages.cpp:52-74 returns false on tx->vin.empty() (:56-62) and batch-adds prevouts after checking them all, with the comment "If we added them 1 at a time, we could catch duplicate inputs within a single tx. This is a more severe, consensus error, and we want to report that from CheckTransaction instead." refs/bitcoin/src/test/txpackage_tests.cpp:192-201 asserts IsConsistentPackage(package_with_dup_tx) is TRUE both alone and with a second member. EXECUTED (dev.sh eval on package-well-formed): (:DUP-INPUT-SINGLE-TX (NIL :CONFLICT-IN-PACKAGE) :EMPTY-VIN-SINGLE (T NIL)) -- both halves of the claim reproduce exactly, in both directions. package-well-formed runs first in validate-package-for-mempool (:578-581) and its failure returns %results-not-validated for every member, so a submitpackage caller really does get a package-level verdict with no per-member attribution where Core gives per-tx "bad-txns-inputs-duplicate". Reachability is narrow: a duplicate-input tx fails CheckTransaction before orphan intake, so it can never enter a P2P 1p1c package -- submitpackage RPC only. No acceptance divergence (both reject; the empty-vin member falls through to validate-transaction-structure -> :no-inputs, no crash path). S3 stands.

### `6dcf6d71` — refuted

**Block-relay-only eviction protection ignores fRelevantServices, so a non-tx-relay peer without the services we want can **

Mechanism is textually true but the stated consequence is unreachable, because our filter is wrong in a DIFFERENT way that makes the pass select nobody at all.

src/node/eviction.lisp:217-224 filters with `(not (bl.net:peer-relays-txs-p p))`. src/networking/peer.lisp:718-723: `peer-relays-txs-p` is derived from OUR side's conn-type — `(not (member (peer-conn-type peer) '(:block-relay :feeler)))` — not from the peer's version fRelay. src/networking/peer.lisp:162 defaults conn-type to :inbound, and src/node/peers.lisp only ever sets :block-relay/:feeler/:addr-fetch on OUTBOUND peers. select-inbound-peer-to-evict (eviction.lisp:264-276) is called only with `(remove-if-not #'peer-inbound (node-peers node))`, so every candidate has conn-type :inbound.

Executed:
  scripts/dev.sh eval '(let ((p (make-peer :address "1.2.3.4" :inbound t))) ...)' => (:CONN-TYPE :INBOUND :RELAYS-TXS T)
  five inbound peers with distinct last-block-times through %evict-erase-last-k with Core's k=8 and our filter => (:N-IN 5 :N-AFTER-BLOCK-RELAY-PASS 5 :ELIGIBLE 0)

So the pass protects ZERO peers; no peer — desirable-services or not — ever consumes one of the 8 reserved slots, and nothing is 'displaced'. The claimed attack (relay=0 plus one block announcement takes a protected slot) cannot occur. The attacker framing would also be weak in Core: fRelevantServices comes from the peer's own self-declared nServices in its version message (net_processing.cpp:3672), so it is free to forge.

Collateral (NOT this finding, but worth filing separately): Core's candidate uses `node->m_relays_txs` = the peer's version fRelay (net.cpp:1700, net_processing.cpp:3695), and we have exactly that predicate available as bl.net:peer-tx-relay-p (peer.lisp:748-760) but do not use it here. The result is that Core's block-relay-only protection pass is dead in our tree — the opposite failure from the one filed. `has-all-desirable-service-flags-p` also already exists (peer.lisp:1053) and is unused here, and our comparator omits Core's m_relay_txs / fRelevantServices / m_connected tie-breaks (eviction.cpp:48-54), but all of that is moot while the filter matches nothing.

### `70502bf3` — S3

**A backed-up send buffer makes us DROP outbound messages; Core instead stops processing inbound messages until it drains**

CONFIRMED, executed. I built a real loopback socket pair in the warm image, set the server fd non-blocking, and pushed 64 KiB chunks through bl.net::send-bytes with the client never reading. Result: (:TOTAL-CALLS 57 :LAST-3 ((65536 979456 NIL) (65536 1044992 T) (NIL 1044992 T)) :DROPPED 1 :STILL-CONNECTED T :QUEUE-AT-DROP 1044992 :PAUSED-P T). So once connection-send-queue-bytes passes +max-send-buffer-bytes+ (1,000,000, /Users/sen/common-lisp/bitcoin-lisp/src/networking/connection.lisp:7), send-bytes returns NIL on a still-connected socket and the message is discarded — not queued, not retried (connection.lisp:492-497). Core side verified: net_processing.cpp:5245 `if (node.fPauseSend) return false;` before PollMessage, ProcessGetData `if (pfrom.fPauseSend) break;` (~2536) leaving the rest in m_getdata_requests, and CConnman::PushMessage (net.cpp:4088-4113) unconditionally does `pnode->vSendMsg.push_back(std::move(msg))` and only SETS fPauseSend as a consequence — Core never discards a message it decided to send. Our tree: grep for connection-send-paused-p over src/ returns exactly two consult sites, protocol.lisp:1994 (handle-getdata) and protocol.lisp:2306 (handle-getblocktxn); drain-and-reap-peer (ibd.lisp:1770-1815) reads and dispatches with no send-pause check at all, so we keep consuming input while dropping output — the exact inversion of Core's backpressure. The getdata half IS documented as a deliberate divergence ("we drop it and the peer re-requests, which its own request timeout already handles"), but that reasoning does not cover the general send path. One consequence the finding did not name is sharper than the ones it did: send-ping (peer.lisp:1394-1399) sets peer-ping-nonce and peer-last-ping-time BEFORE send-message, and ignores its NIL return, so a ping dropped by a paused buffer arms a nonce that can never be answered and check-peer-health (peer.lisp:1483-1486) then disconnects that healthy peer deterministically +ping-timeout-seconds+ = 1200 s later. Severity left at S3: the blast radius is one already-backlogged peer connection, recovery is automatic (replace-disconnected-peers), and nothing consensus-facing or remotely amplifiable is touched — a reviewer could argue S2 for the deterministic peer drop, but it is bounded and self-healing.

### `7b813f60` — S3

**generatetoaddress / generatetodescriptor raise an error when maxtries is exhausted instead of returning the blocks mined**

CONFIRMED (mechanism executed). /Users/sen/common-lisp/bitcoin-lisp/src/rpc/mining.lisp:606-631 (%generate-to-script-pubkey): a `dotimes` over nblocks, each iteration calling `(bl.mining:mine-block block :max-tries maxtries)` and, on NIL, `(error 'rpc-error :code +rpc-misc-error+ :message "Failed to find a valid nonce")` — so maxtries is a fresh per-block budget, not the shared `uint64_t& max_tries` Core threads through generateBlocks, and the `hashes` accumulator is discarded by the non-local exit even though those earlier blocks were already activated. maxtries reaches it as `(or (third params) 1000000)` (:640, :663), and 0 is truthy in Lisp, so `generatetoaddress(1, addr, 0)` really does arrive as max-tries 0. Executed: `(bl.mining:mine-block b :max-tries 0)` => NIL (and :max-tries 1 => NIL), so the error fires. Core returns `[]` instead: mining.cpp:142-148 `while (max_tries > 0 ...)` never runs, `if (max_tries == 0 || chainman.m_interrupt) return false`, and generateBlocks (mining.cpp:167-181) treats false as `break` and returns the accumulated array. DEFAULT_MAX_TRIES = 1000000 (rpc/mining.h:9) matches ours.

The interrupt half also holds: src/mining/builder.lisp:122-132 is a bare `(dotimes (nonce (min max-tries #x100000000)) ...)` with no call to bl.ctx:interrupt-requested-p / bl:*interrupt-check* (grep over src/ finds the special defined in src/util/context.lisp:69 and installed in src/node/shutdown.lisp:165, and no reference from src/mining/).

Severity unchanged at S3: reachable only when a caller deliberately passes a small maxtries (on regtest nonce 0 almost always solves, and the 1e6 default grind is seconds, not a shutdown-length hang), and the chain state is still correct — only the reply is lost. Worth noting a same-family divergence the filing does not mention: our handlers additionally reject nblocks <= 0 with -8 (mining.lisp:637-639, :660-662), where Core's `while (nGenerate > 0)` simply returns `[]`.

### `7db4b27f` — S3

**verify-checksig's inline FindAndDelete pattern omits the PUSHDATA prefix for signatures longer than 75 bytes**

Code confirmed verbatim at src/coalton/interop.lisp:2147-2152 — `(if (<= siglen 75) (concatenate ... (vector siglen) sig-bytes) sig-bytes)` — and the same truncated shape in the CONST_SCRIPTCODE probe at :2116-2119, where the pattern is only built `(when (<= siglen 75) ...)`. strip-sigs-from-script-code (interop.lisp:2403-2422) does get all three encodings right (75 / 0x4c / 0x4d), so the two call sites in the same file genuinely disagree, as claimed. Core's `CScript() << vchSig` emits `4c <len>` for 76..255 via AppendDataSize (script.h:407-416). EXECUTED: with *script-flags* = "CONST_SCRIPTCODE", scriptSig `4b <75 bytes>` against scriptPubKey `4b <same 75 bytes> 75 21 <pk> ac` returns (NIL :ERROR) — the FindAndDelete match is detected — while the 76-byte twin (`4c 4c <76 bytes>` in both) returns (NIL :EVAL-FALSE), i.e. the match is missed exactly as described. Consequence stands at nil, and is in fact slightly weaker than the write-up implies: the mutated scriptCode only ever feeds a CHECKSIG whose >75-byte sig can never parse as DER, so the boolean is false either way; and the missed :sig-findanddelete needs CONST_SCRIPTCODE ON with DERSIG OFF, a combination that exists in neither +standard-script-verify-flags+ (DERSIG is mandatory there) nor any block flag set (CONST_SCRIPTCODE is never a block flag). Real latent defect in consensus-critical code, no reachable behavioural divergence. S3.

### `7f01522e` — S2

**%scan-flat-block-files stops at the first missing blk file, so a pruned node loses its entire flat block index on restar**

MECHANISM CONFIRMED BY EXECUTION. src/storage/blocks.lisp:544-546 is literally `(loop for file from 0 for path = (flat-file-name seq (make-flat-file-pos file 0)) while (probe-file path) ...)`, and its sibling %scan-flat-undo-files (blocks.lisp:310-341) enumerates with %existing-rev-file-numbers precisely because "rev numbering has holes. Pruning deletes the lowest-numbered files first" — the blk scanner never got the same treatment. Executed probe: stored two blocks into blk00000.dat and blk00001.dat in /tmp/bl-scan-probe-1, deleted blk00000.dat, re-ran (bitcoin-lisp.storage:init-block-store dir) → (:FILES-ON-DISK ("blk00001.dat") :INDEX-COUNT 0 :EXISTS-AFTER NIL :GET-BLOCK-AFTER NIL :CURSOR (0 0) :TOTAL-BYTES 0). So a surviving higher-numbered file is fully invisible after restart, the cursor rewinds to (0,0), and total-bytes reads 0. NOT HANDLED ELSEWHERE: get-block (blocks.lisp:443) and block-exists-p (blocks.lisp:516) read only block-store-index; block-index-entry-file/data-pos are WRITTEN (chain.lisp:365-366) and persisted in the v2 header index (chain.lisp:983-985, 1346-1364) but the only reader is reindex-block-index behind the explicit -reindex flag (init.lisp:620-631) — nothing repopulates block-store-index from them at ordinary startup. Reachability re-checked and is WORSE than the report states: prune-old-blocks passes (1+ (chain-state-prune-walk-start ...)) as min-height and pruned-height starts at 0 (chain.lisp:57), so blk00000.dat (height-first 0, genesis written by ensure-genesis-on-disk) is never prunable and the first deleted file is blk00001.dat — the hole is permanent and ensure-genesis-on-disk cannot plug it. rebuild-block-file-info (blocks.lisp:706-740) then joins an empty index, so file-info is empty, total-bytes under-reports and automatic pruning stops. *flat-block-files* has defaulted to T since 2026-08-26 (blocks.lisp:25). The write-overwrite tail of the impact needs a full 128MiB rollover to reach a retained higher file, so that part is a further consequence rather than the immediate one, but the index/availability loss is immediate. S2 stands.

### `801f2ad3` — S2

**-rpcpassword / -rpcauth / -rpcuser / -torpassword are written to debug.log in cleartext; Core masks them as ******

CONFIRMED, executed. Core: init.cpp:602 (-torpassword), :707 (-rpcauth), :712 (-rpcpassword), :716 (-rpcuser) all carry ArgsManager::SENSITIVE, and common/args.cpp:883 in logArgsPrefix does `std::string value_str = (*flags & SENSITIVE) ? "****" : value.write();` for every `Config file arg:` / `Command-line arg:` line. Ours: src/node/init.lisp:1479-1497 %log-args emits `Config file arg: ...~A=~A` and `Command-line arg: ~A=~A` with the raw JSON rendering, gated only by known-config-option-p — no sensitivity notion anywhere. grep over src/ for '****', 'sensitive', 'redact', 'mask' returns NOTHING, so the check has not merely MOVED. src/config/args.lisp:180-195 cli-arg-log-cells and src/config/conf.lisp:185 config-arg-log-cells both return the value verbatim. All four are registered options (src/config-options.lisp:53 rpcuser, :54 rpcpassword, :63 torpassword, :126 rpcauth). %log-args is reached from the live startup path (init.lisp:1582), and defer-log :info (src/logging.lisp:547) is flushed to debug.log by flush-deferred-log-lines. EXECUTED: (cli-arg-log-cells '("-rpcpassword=hunter2" "-rpcuser=alice" "-torpassword=tsecret" "-rpcauth=alice:deadbeef$cafe")) returns (("rpcpassword" . "\"hunter2\"") ("rpcuser" . "\"alice\"") ("torpassword" . "\"tsecret\"") ("rpcauth" . "\"alice:deadbeef$cafe\"")) and known-config-option-p is T for all four — so every one of them is logged in plaintext. Note the tree elsewhere KNOWS this is secret: src/rpc/server.lisp:1104 documents 'The spec is never logged: it names a user and carries the password's HMAC' for the -rpcauth error path, which makes the %log-args leak an inconsistency rather than a considered divergence. S2 stands.

### `81c98c4b` — S3

**enforce_BIP94 is hardcoded to testnet4; Core lets regtest turn it on with -test=bip94**

CONFIRMED, executed, and the divergence is slightly WIDER than filed. Read: src/validation/block.lisp:629-643 gates on `(eq bl:*network* :testnet4)` with no option; "test" appears in src/config-options.lisp:407 inside define-core-only-options (accepted, not implemented), so `-test=bip94` is silently ignored. Core side verified verbatim: refs/bitcoin/src/kernel/chainparams.cpp:579 `consensus.enforce_BIP94 = opts.enforce_bip94;` in the regtest block, refs/bitcoin/src/chainparams.cpp:47 `if (HasTestOption(args, "bip94")) options.enforce_bip94 = true;`, and validation.cpp:4127-4137 whose comment reads "Testnet4 and regtest only".

EXECUTED PROOF (scripts/dev.sh eval): a header 7200s before its predecessor's actual time at height 2016 with prev at 2015:
  (:INTERVAL 2016 :MAX-TIMEWARP 600 :TESTNET4 T :REGTEST NIL :MAINNET NIL)
Regtest never violates. SECOND, UNFILED HALF: bl.store:+difficulty-adjustment-interval+ is a flat defconstant 2016 (src/storage/chain.lisp:424), whereas Core's regtest DifficultyAdjustmentInterval() is 144 (nPowTargetTimespan 24*60*60 / nPowTargetSpacing 600; mining_basic.py:53 hardcodes 144). So even the mining-side BIP94 floor we DO apply on all networks (src/mining/assembler.lisp:83-95, whose comment already documents this: "ours uses the fixed 2016 ... except regtest (Core: 144), where BIP94 is off and the extra floor is moot") fires at the wrong heights on regtest.

IMPACT CONFIRMED AS STATED: refs/bitcoin/test/functional/mining_basic.py:193-248 is the only test using -test=bip94; it restarts on regtest, asserts tmpl['mintime'] == tmpl['curtime'] >= t + MAX_FUTURE_BLOCK_TIME - MAX_TIMEWARP at a height that is a multiple of 144, and asserts submitheader raises -25 'time-timewarp-attack'. Against us submitheader would succeed, so that test cannot run and the rule has no regtest coverage. No consensus effect on mainnet/testnet3/testnet4/signet. Severity S3 stands.

### `83599047` — S3

**Block templates never signal any BIP9 deployment: nVersion is the hardcoded constant 0x20000000 and vbavailable is hardc**

CONFIRMED, EXECUTED. `scripts/dev.sh eval '(list bitcoin-lisp.mining::+versionbits-top-bits+ (bitcoin-lisp.mining:block-template-version (bitcoin-lisp.mining::make-block-template)))'` -> (536870912 536870912) = 0x20000000. Read src/mining/assembler.lisp:54-56 (the defconstant) and :72 (the defstruct slot default), and the ONLY constructor, assembler.lisp:264-270, which never passes :version — so nothing can ever set it. `grep -rn 'ComputeBlockVersion|compute-block-version|blockversion' src/` returns exactly one hit, src/config-options.lisp:391, where 'blockversion' sits in the ignored core-only list — confirming both halves of the claim (no ComputeBlockVersion equivalent exists, and -blockversion is a no-op). getblocktemplate at src/rpc/mining.lisp:384 emits that constant, and :417-418 hardcode an empty vbavailable / vbrequired 0.

Core side verified directly: refs/bitcoin/src/versionbits.cpp:265-279 ORs the mask for every deployment in STARTED or LOCKED_IN; refs/bitcoin/src/node/miner.cpp:140-145 assigns that to pblock->nVersion and applies the -blockversion override on MineBlocksOnDemand chains; refs/bitcoin/src/rpc/mining.cpp:958-987 fills vbavailable and re-emits block.nVersion.

The impact's regtest argument holds: refs/bitcoin/src/kernel/chainparams.cpp:582-587 gives regtest testdummy bit 28, nStartTime 0, NO_TIMEOUT, threshold 108 / period 144 — permanently STARTED (never ALWAYS_ACTIVE, never NEVER_ACTIVE, no timeout to fail into), so Core's regtest template is 0x30000000. Our own table transcribes the same values at src/validation/versionbits.lisp:74-76. mining_basic.py:85 and :87 assert the -blockversion override and VERSIONBITS_TOP_BITS + (1<<28); both fail against us.

Severity stays S3: I checked the other four chains and the in-code justification is correct for them — mainnet/testnet3/signet testdummy are NEVER_ACTIVE and taproot is ACTIVE or FAILED on all of them, so Core also emits exactly 0x20000000 there. The divergence is regtest-only today; the forward-looking half (no plumbing for a future BIP9 fork) is a real but unrealized gap.

### `87801e86` — S2

**Network-only options (-port, -rpcport, -bind, -connect, -addnode, -wallet, -walletdir) are honored from bitcoin.conf's d**

Read + EXECUTED. Our side: args->start-node-plist (src/node/args.lisp:246-287) builds `conf` = (all files' [network] sections) appended to (all files' globals) and `merged` = cli + settings-cells + conf, first ASSOC wins. There is no notion of a network-only option and no unsuitable-section check anywhere: `grep -rni "network-only|network_only|only applied on|unsuitable" src/` returns one unrelated hit in src/serialization/messages.lisp:703. Executed proof, CLI -testnet4 with a conf whose GLOBAL area holds mainnet values (rpcport=8332, port=8333, connect=1.2.3.4, addnode=5.6.7.8, wallet=mainwallet, plus a [testnet4] section holding only maxconnections=40): the returned plist is (:WALLET T :WALLET-NAMES ("mainwallet") :CONNECT-NODES ("1.2.3.4") :ADDNODE ("5.6.7.8") :PORT 8333 :RPC-PORT 8332 :MAX-CONNECTIONS 40 :NETWORK :TESTNET4) -- every network-only value from the default section applied on testnet4. Core side verified in the tree: ArgsManager::UseDefaultSection (refs/bitcoin/src/common/args.cpp:854-857) returns false for a network-only arg whenever m_network != main, and its negation is passed as ignore_default_section_config into GetSetting/GetSettingsList (:452-453, :863-864, :870); GetUnsuitableSectionOnlyArgs (:134-146) collects any network-only arg set only in the default section, and init.cpp:944-951 turns that into `InitError("Config setting for %s only applied on %s network when in [%s] section.")` -- a refusal to start. All seven named options really do carry ArgsManager::NETWORK_ONLY: init.cpp:539 (-addnode), :548 (-bind), :550 (-connect), :575 (-port), :708 (-rpcbind), :713 (-rpcport), wallet/init.cpp:71 (-wallet), :73 (-walletdir). Consequence is reachable and matches the claim: a shared bitcoin.conf makes our testnet node bind RPC 8332 (collides with a mainnet node's RPC) and, via -connect to a mainnet peer, isolates the testnet node entirely (-connect also suppresses dnsseed/auto-connections). S2 unchanged.

### `88f3a071` — S3

**Block-conflict removal does not clear the conflicted transaction's prioritisation delta**

CONFIRMED, executed. refs/bitcoin/src/txmempool.cpp:388-403 removeConflicts calls ClearPrioritisation(txConflict.GetHash()) before removeRecursive; ClearPrioritisation (:667) erases the mapDeltas entry. Ours (src/mempool/mempool.lisp:1755-1793): the :block branch does (remhash txid (mempool-deltas mempool)) per confirmed tx; the :conflict branch calls only mempool-remove-recursive. grep confirms exactly one remhash of mempool-deltas in the whole file (:1779, the :block branch) plus the net-zero drop in mempool-prioritise (:279). EXECUTED: prioritised an in-pool tx by 100000, then mempool-remove-for-block with a block tx double-spending its input -> (:DELTAS-BEFORE 1 :DELTAS-AFTER 1 :MEMPOOL-COUNT-AFTER 0 :RESIDUAL-DELTA 100000 :USAGE-BEFORE 800 :USAGE-AFTER 96). The tx is gone, the delta survives, and 96 bytes (+usage-per-delta+) stay in mempool-dynamic-usage. Downstream reach verified by reading: %core-mempool-payload (:1460-1470) pushes any delta whose txid is not in the pool into the mempool.dat residual section (so it reloads on restart), and getprioritisedtransactions (src/rpc/mining.lisp:872-893) maphashes mempool-deltas and would report it with in_mempool=false where Core reports nothing. Not an upgrade: deltas exist only by operator prioritisetransaction (or mempool.dat reload), so the growth is operator-bounded, not attacker-driven; no consensus or DoS reach. Note our RBF-replacement path also leaves the delta, which is CORRECT -- Core's removeForReplacement does not ClearPrioritisation either (mining_prioritisetransaction.py:90-102, "tx prioritisation stays after a tx is replaced"). S3 stands.

### `8d88f6ac` — S3

**base58-decode has no length bound; Core's DecodeBase58 bails as soon as the decoded length exceeds max_ret_len**

CONFIRMED, executed and measured. Timing bitcoin-lisp.crypto::base58-decode on all-'z' strings in the warm image: ((1000 :MS 1) (10000 :MS 7) (50000 :MS 200) (100000 :MS 723) (200000 :MS 2901)) — cleanly quadratic (2x length -> 4x time), from the unbounded `num = num*58 + digit` bignum accumulation at /Users/sen/common-lisp/bitcoin-lisp/src/crypto/address.lisp:50-58. End to end through the address entry point: (:DECODE-ADDRESS-200K-MS 2812 :RESULT NIL), so bech32 does not short-circuit it. Core side verified: base58.cpp:40-71 DecodeBase58 takes max_ret_len and bails inside the loop (`if (length + zeroes > max_ret_len) return false;`), with key_io.cpp:93 passing 21 for destinations — Core returns after ~30 characters. The whitespace half is confirmed too: decode-address " 1BvBMSEYstWetqTFn5Au4m4GFg7xJaNVN2 " -> NIL while the untrimmed string -> :P2PKH, whereas Core skips leading (base58.cpp:42-43) and trailing spaces, so the same input is valid there and invalid here. Reachability: rpc-validateaddress (src/rpc/output-script.lisp:132-137) passes the argument straight to decode-address with no length check, and +max-rpc-body-size+ is 32 MiB (src/rpc/server.lisp:667), so a single request can hand it a multi-megabyte string — extrapolating the measured curve, ~70 s of one handler thread per 1 MB argument, and a CPU-bound handler cannot be cut short by *rpc-server-timeout* (a socket timeout). I checked src/rpc/rest.lisp and src/rpc/ui.lisp: neither takes an address, so there is no unauthenticated path — authenticated RPC only, and an authenticated caller already commands the node. Severity unchanged at S3, with the note that the cost is real and measured rather than theoretical.

### `97855c61` — S2

**PSBT "complete" and the already-signed test trust final/partial fields without verifying the scripts**

CONFIRMED and demonstrated by execution. Core computes complete as the AND of PSBTInputSignedAndVerified over every input (wallet.cpp:2231-2235), which resolves the UTXO and runs VerifyScript under STANDARD_SCRIPT_VERIFY_FLAGS (psbt.cpp:325-355), and returns false outright when neither non_witness_utxo nor witness_utxo is present; SignPSBTInput uses the same predicate at psbt.cpp:406-408 to decide an input needs no work. Ours: %psbt-input-signed-p (src/wallet/psbt.lisp:996-1002) returns true on mere PRESENCE of the final fields, %psbt-record-signatures skips such inputs (:1245-1249), and %psbt-signer-result (:1418-1429) derives complete from %psbt-finalize-in-place on a copy, i.e. from whether %psbt-finalize (:726-790) can assemble bytes — it never verifies, and %psbt-multisig-sigs only counts sigs by pubkey against m. Executed: built a 1-in/1-out PSBT via bl.ser:make-empty-psbt, set +psbt-in-final-scriptsig+ to a single OP_1 byte with NO utxo record at all, and called bl.wallet::%psbt-signer-result — it returned ("complete" . T) plus a "hex" of the assembled transaction. Core returns complete:false for that input on the missing-UTXO branch alone. Reachable from walletprocesspsbt/descriptorprocesspsbt, whose responses are exactly this function (:1459, :1564): a counterparty-supplied garbage final_scriptSig makes us both skip signing that input AND report complete, and a malformed co-signer partial sig is assembled the same way. Upgrading to S2: unlike the other three this is not a lost warning but a wrong answer in an RPC's documented output plus an unusable `hex` the caller is invited to broadcast; no funds are at risk and the node rejects the tx, which is why it is not higher.

### `98069fe2` — S3

**sendcmpct(0) never clears the recorded high-bandwidth-from flag**

MECHANISM CONFIRMED BY EXECUTION. src/networking/protocol.lisp:3255-3261 HANDLE-SENDCMPCT: `(when (= version +compact-blocks-version+) (setf (peer-compact-block-version peer) version) (when high-bandwidth (setf (peer-compact-block-high-bandwidth peer) t)))` — one-directional. refs/bitcoin/src/net_processing.cpp:3916-3919 assigns unconditionally (`nodestate->m_requested_hb_cmpctblocks = sendcmpct_hb; pfrom.m_bip152_highbandwidth_from = sendcmpct_hb;`). Ran it: peer fed sendcmpct(hb=1,v=2) then sendcmpct(hb=0,v=2) -> (:AFTER-SENDCMPCT-1 T :AFTER-SENDCMPCT-0 T). Not handled elsewhere: `grep -rn compact-block-high-bandwidth src/` gives exactly three non-test sites — protocol.lisp:3147 (%SET-PEER-HB, which writes the *-TO* slot, Core m_bip152_highbandwidth_to), protocol.lisp:3260 (the buggy write), and src/rpc/net.lisp:247 (getpeerinfo "bip152_hb_from"). CONSEQUENCE HOLDS AND IS BOUNDED EXACTLY AS FILED: the _from slot has no other reader, and RELAY-BLOCK (protocol.lisp:3053-3073) announces with headers-or-inv only and never consults it, so nothing but the getpeerinfo field can go wrong today. The demotion is reachable in the wild rather than theoretical: Core itself sends sendcmpct(false,2) to the peer it evicts from lNodesAnnouncingHeaderAndIDs (MaybeSetPeerAsAnnouncingHeaderAndIDs), so any Core peer that promotes us and later evicts us leaves our bip152_hb_from stuck T for the life of the connection. Diagnostic-only today = S3.

### `994f223b` — S3

**The relay-finality check runs after the duplicate and missing-input checks, so a non-final transaction with unknown pare**

CONFIRMED, executed, with one clause of the impact refuted.

Order confirmed on both sides. Core validation.cpp:813-815 tx-size-small, :819-821 CheckFinalTxAtTip -> TX_PREMATURE_SPEND 'non-final', :823 m_pool.exists, :866 TX_MISSING_INPUTS. Ours: src/validation/transaction.lisp:1078-1081 tx-size-small, 1084-1088 duplicate, 1094-1120 mempool-extra-coins -> :missing-input, and only at 1130-1139 the :non-final check (BIP68 at :1141-1144 correctly matches Core's late position at validation.cpp:887).

Executed: a tx with nLockTime=4000000000 and sequence 0 spending an unknown outpoint against an empty utxo-set ->
  (NIL :MISSING-INPUT NIL)
Core returns 'non-final' for the same transaction.

Consequence chain confirmed: src/networking/protocol.lisp:1789-1801 branches on `(eq error :missing-input)` to orphan-add + request-orphan-parents, while every other error falls to the %cache-tx-rejection branch. Core txdownloadman_impl.cpp:361 routes only TX_MISSING_INPUTS to the orphanage and :468 inserts every other failure's wtxid into RecentRejectsFilter. So a non-final tx with unknown parents does enter our orphanage and does draw parent getdatas that Core never issues.

Severity: stays S3. No attacker capability is added — a FINAL tx naming nonexistent parents already lands in the orphanage in both implementations, and the orphanage weight and per-peer parent-request budgets bound it identically. The impact's last clause is wrong: mempool_accept.py:390-397 builds its 'non-final' case from raw_tx_reference, whose inputs are confirmed, so our order reaches the finality check and reports non-final too (both testmempoolaccept and sendrawtransaction pass :chain-state, src/rpc/mempool.lisp:557 and :660). No Core functional test matches on the diverging both-non-final-and-missing-parents case.

### `9f54cae8` — S3

**%reorg-commit fires tx-relay and wallet side effects without connect-block's background-chainstate role guard**

MECHANISM PARTLY CONFIRMED, IMPACT HALF REFUTED. Read src/validation/block.lisp:2676-2699 (connect-block's `(unless (bl.store:chain-state-target-blockhash chain-state) ...)` wrapping mempool-expire / orphan-erase-for-block / note-block-connected / wallet-notify-block-connected / maybe-stop-at-height) against %reorg-commit at :3300-3400 and perform-reorg's preamble at :3414-3470 — there is indeed NO target-blockhash test anywhere on the reorg path, so reset-recent-confirmed (:3319), orphan-erase-for-block (:3365), note-block-connected (:3367), mempool-remove-for-block (:3362) and readd-disconnected-txs-to-mempool (:3394) run for whatever chainstate is advancing. That much stands.

BUT the two loudest consequences in the impact do not follow, because the callees carry their OWN role guards, which the finding missed:
- src/node/wallet-hooks.lisp:19-45 — `wallet-notify-block-connected` and `wallet-notify-block-disconnected` both gate on `(not (bl.store:chain-state-target-blockhash chainstate))` with a docstring citing wallet.cpp:1526-1529. The wallet is therefore NOT 'told ancient blocks just connected'; that clause of the title and impact is refuted.
- src/node/indexes.lisp:227-240 — `index-block-connected` drops any chainstate that is not `(node-validated-chainstate *node*)`, so the index writes in the same loop are role-gated too.

What is left unguarded is only the tx-relay pair: `note-block-connected` (src/validation/block.lisp:2502, rebuilds *most-recent-block-txs* and inserts into *recent-confirmed-txs*) and `reset-recent-confirmed` (:2460), plus mempool operations against the node's single shared mempool.

Reachability: activate-block's target filter (:3937-3956) confines a targeted chainstate to target-ancestor blocks, but it does not confine it to case 1 — a block at H+2 arriving while the historical tip is H, with H+1's body already on disk, takes case 2 (:4000-4007) and calls perform-reorg on the historical chainstate as a zero-disconnect fast-forward. So the path is not provably dead, contra the finding's stronger phrasing; the residual effect is an ancient block becoming the relay 'most recent block' and a filter reset, which is transient and self-correcting. No observable defect, so S3 is the right level as filed. I did not exercise it (no assumeutxo background chainstate fixture available to drive), hence executed=false.

### `a4680ae1` — S3

**Gossiped addresses are relayed immediately, one addr message per address — no m_addrs_to_send queue and no poisson broad**

MECHANISM CONFIRMED and reproduced by execution; CONSEQUENCE only partly follows, so I downgrade S2 -> S3.

Mechanism: relay-address (src/networking/protocol.lisp:1108-1156) ranks eligible peers and, inside the same loop, calls (build-addr-response p (list peer-addr)) then (send-message p msg) -- one addr message per address per destination, emitted from the addr/addrv2 handler's own pass (%process-gossiped-addresses -> protocol.lisp:1339). No queue and no timer exist: the only *-addr-send state in the tree is peer-next-local-addr-send (peer.lisp:152, protocol.lisp:2631-2638), which drives the separate ~24h local self-announcement; grep for addrs-to-send / maybe-send-addr / next-addr-send returns nothing else, and the sync-loop pump (init.lisp:930-990) has a Poisson flusher for tx invs and for the local address but none for gossip relay.

EXECUTED (two real loopback connections, relay-address called once with a fresh gossiped IPv4 record):
  (:TARGETS 2 :BYTES-ON-WIRE-IMMEDIATELY (55 55) :QUEUED (0 0) :ADDRS-TO-SEND-SLOT NIL)
55 bytes = 24-byte header + varint + one 30-byte addr entry, accepted by the kernel synchronously inside the relay-address call. So the send is immediate, not queued.

core_ref verified: AVG_ADDRESS_BROADCAST_INTERVAL{30s} at net_processing.cpp:160, PushAddress appending to m_addrs_to_send at :1135-1138, MaybeSendAddr flushing the whole vector as one message and re-arming m_next_addr_send with rand_exp_duration at :5571-5599.

Severity correction: of the three stated impacts only the timing one survives. (a) Timing correlation -- real, and it is the stated purpose of Core's exponential timer, but it is a passive topology-inference aid with a 1-2 peer fan-out, not an exploitable DoS/eclipse/consensus weakness, and our tx-relay side already has its Poisson trickle. (b) 'multiplies bandwidth/message count' -- Core sends the SAME addresses to the SAME 2 peers, just batched; the delta is ~25 bytes of framing per address, negligible. (c) 'per-message amplification: 10 addresses become 20 outbound addr messages' -- not amplification: Core also relays each of those 10 addresses to 2 peers, and our relay is gated to unsolicited messages of <=10 addresses (protocol.lisp:1337-1339) with the same per-address token bucket, so the outbound address COUNT is identical. Real divergence, correctly located, but S3.

### `a6afe2d6` — S3

**generateblock builds its own coinbase and re-introduces the unconditional segwit witness that builder.lisp already fixed**

CONFIRMED, EXECUTED end to end. src/rpc/mining.lisp:754-758 calls `(bl.mining:build-coinbase-transaction height subsidy :script-pubkey ... :witness-commitment-script (%witness-commitment-script-for-txs txs))` with no :segwit-active, so src/mining/builder.lisp:16 default `(segwit-active t)` applies and builder.lisp:63-67 attaches the reserved value. Verified in the image: `(bitcoin-lisp.serialization:transaction-has-witness-p (bitcoin-lisp.mining:build-coinbase-transaction 5 5000000000 :witness-commitment-script <38 zero bytes>))` -> T, witness = #((#(0 x32))). Then `(bitcoin-lisp.validation::validate-witness-commitment <block with that coinbase> nil)` -> (NIL :UNEXPECTED-WITNESS), the exact rejection claimed.

The sibling site is gated correctly (builder.lisp:96-100, `:segwit-active (>= height (bl.val:get-segwit-activation-height bl:*network*))`), and builder.lisp:23-38 documents this precise bug — so the 'the fix never reached the second construction site' framing is accurate. Core's split confirmed at refs/bitcoin/src/validation.cpp:4016-4026 (UpdateUncommittedBlockStructures gates the witness on DeploymentActiveAfter(..., DEPLOYMENT_SEGWIT)) vs :4028-4049 (GenerateCoinbaseCommitment adds the OUTPUT unconditionally); rpc/mining.cpp:389 is the RegenerateCommitments call.

Consequence chain verified: src/validation/block.lisp:249-255 runs validate-witness-commitment inside the contextual check with segwit-active = (>= height (get-segwit-activation-height *network*)), and test-block-validity (block.lisp:1922-1949) runs the full VALIDATE-BLOCK battery, so the dry run at src/rpc/mining.lisp:773-778 raises RPC_VERIFY_ERROR (-25). Reachable only via -testactivationheight, which we do support (src/config-options.lisp:128; src/node/init.lisp:190 restricts it to regtest) — regtest's default segwit-height is 0 (src/util/chainparams.lisp:287), so the bug is invisible without that flag. Regtest-only reach keeps this at S3.

### `a7b67ad1` — S3

**OP_CHECKLOCKTIMEVERIFY / OP_CHECKSEQUENCEVERIFY consult DISCOURAGE_UPGRADABLE_NOPS when their own flag is off; Core trea**

Core at d3056bc verified: interpreter.cpp:521-526 (CLTV) and :560-565 (CSV) are `if (!(flags & SCRIPT_VERIFY_...)) { /* not enabled; treat as a NOP2|NOP3 */ break; }` with no DISCOURAGE check; SCRIPT_VERIFY_DISCOURAGE_UPGRADABLE_NOPS appears only in the OP_NOP1/OP_NOP4..OP_NOP10 case at :594-599. Ours at src/coalton/script.lisp:2107-2110 and :2141-2144 calls check-discouraged-nop (script.lisp:1065-1074), which returns SE-DiscourageUpgradableNops. EXECUTED: with *script-flags* = "DISCOURAGE_UPGRADABLE_NOPS", scriptSig `51` / scriptPubKey `b1` (NOP2) => (NIL :ERROR) and `b2` (NOP3) => (NIL :ERROR), where Core returns OK; with flags "" both return (T NIL), confirming the discourage flag is what flips them. Consequence confirmed unreachable: +standard-script-verify-flags+ prints as "P2SH,DERSIG,NULLDUMMY,CHECKLOCKTIMEVERIFY,CHECKSEQUENCEVERIFY,WITNESS,TAPROOT,STRICTENC,..." so CLTV/CSV are always on wherever DISCOURAGE is, and the block set (block.lisp:288-332) never contains DISCOURAGE at all — the two conditions cannot co-occur. The claim that script_tests.json has no covering vector also checks out: grep shows DISCOURAGE_UPGRADABLE_NOPS only at lines 251, 253 and 886-898, covering NOP, NOP1 and NOP4..NOP10 — never NOP2/NOP3 — so the green suite is indeed no evidence. S3 unchanged.

### `a9ddbd92` — S3

**getnetworkhashps computes the wrong number: integer rounding drops sub-1 H/s results to 0, and the timespan uses the win**

CONFIRMED, EXECUTED. src/rpc/mining.lisp:809-819 reads verbatim as claimed: dtime is `(- tip.header.timestamp start.header.timestamp)` — the two ENDPOINTS only — and the return is `(if (<= dtime 0) 0 (round dwork dtime))`. CL's ROUND on two integers returns an integer; verified in the image: `(list (round 2 600) (round 1000 600))` -> (0 2). So both halves of the mechanism hold: sub-0.5 H/s collapses to 0 and everything else truncates to whole hashes/second.

Core contrast verified at refs/bitcoin/src/rpc/mining.cpp:91-108: it walks the window accumulating minTime/maxTime, returns 0 only when they are equal, and returns `workDiff.getdouble() / timeDiff` — a double. The regtest arithmetic in the impact checks out (regtest work per block = 2, spacing 600 -> 2/600 = 0.00333, and (round 2 600) = 0), so mining_basic.py:399 (`round(mining_info['networkhashps'], 5) == Decimal('0.00333')`) and rpc_blockchain.py:534-536 (`abs(hps*300 - 1) < 0.0001`) both fail against us. getmininginfo reuses the same function (src/rpc/mining.lisp:461), so it is wrong there too. The non-monotonic-timestamp half also follows from the code: a tip timestamp <= the window-start timestamp yields dtime <= 0 and a hard 0, where Core's max-min is still positive.

Two further divergences in the same function that the finding did NOT claim, noted for completeness: nblocks handling `(if (and (integerp n) (> n 0)) n 120)` silently defaults instead of throwing RPC_INVALID_PARAMETER on 0 (rpc_blockchain.py:525-528 asserts that -8), and neither the -1 'since last difficulty change' form nor the second `height` parameter is implemented, though both are advertised in src/rpc/core-tables.lisp:59.

Severity stays S3: the value is informational, and on every real network (work per block ~1e13 and up) integer truncation is immaterial — the wrong answer is confined to low-difficulty chains, i.e. regtest, plus the two functional-test assertions.

### `abae237b` — S2

**Eviction's disadvantaged-network reserve protects the NEWEST connections instead of the longest-connected (comparator in**

CONFIRMED, executed. src/node/eviction.lisp:151-158 passes `(lambda (a b) (< (peer-connect-time a) (peer-connect-time b)))` to %evict-erase-last-k for the per-network reserve; %evict-erase-last-k (:69-81) protects `(subseq sorted (- (length sorted) n))`. peer-connect-time is `internal-real-time at connection` (src/networking/peer.lisp:245), so larger = more recent: ascending sort puts the newest LAST, i.e. protected. Core's CompareNodeNetworkTime ends `return a.m_connected > b.m_connected` (eviction.cpp:64-72) and its own doc comment says the order is "from most recently connected to connected longer", so EraseLastKElements protects the LONGEST-connected of that network — the same direction as ReverseCompareNodeTimeConnected used by the uptime half. Our uptime pass two lines later (:167-171) correctly uses `>`, so the two passes in the same function disagree, and the file's own docstring ("The comparator orders WORST-first, so the last K are the best K") is violated by the `<`. No comment claims a deliberate divergence.

Executed in the warm image: 16 inbound candidates = 8 clearnet (connect-time 1..8, oldest) + 4 long-lived onion (10..13) + 4 fresh onion (20..23). `%evict-protect-by-ratio` returned still-evictable = (5 6 7 8 10 11 12 13): the four FRESH onion peers were protected and the four long-lived onion peers were left in the eviction pool. Hand-evaluating Core on the same input (sort puts non-onion first, then onion descending by m_connected; last k=4 = onion 10..13) protects the long-lived four and leaves 20..23 evictable — the exact inverse. Reachability: merge-inbound-peers -> evict-least-valuable-inbound -> select-inbound-peer-to-evict -> %evict-protect-by-ratio, live on every inbound admission at capacity. The cited tests only assert `(plusp left)` (dos-protection-tests.lisp:596-619), so they pass either way. Consequence follows: the reserved quarter of inbound slots for onion/I2P/CJDNS/localhost goes to whoever connected most recently, inverting Core's "precludes attacks that start later" property; note the damping that a long-lived disadvantaged peer can still be picked up by the general longest-uptime half when few other old peers compete (my test shows it is crowded out as soon as they do). S2 kept.

### `b219c779` — S3

**getrawtransaction consults the mempool even when an explicit blockhash is given, and never rejects an unknown blockhash**

Both halves confirmed, one of them by execution. Core: refs/bitcoin/src/node/transaction.cpp:143-145 reads `if (mempool && !block_index) { CTransactionRef ptx = mempool->get(hash); if (ptx) return ptx; }` — the mempool is skipped whenever a block index was supplied; and refs/bitcoin/src/rpc/rawtransaction.cpp:298-305 resolves params[2] FIRST under cs_main and `if (!blockindex) throw JSONRPCError(RPC_INVALID_ADDRESS_OR_KEY, "Block hash not found");` before GetTransaction is called at all. Ours: src/rpc/rawtransaction.lisp binds `mempool-entry` at :46-48 and returns it unconditionally at :50-56, BEFORE the `(when blockhash-hint ...)` branch at :58; that branch only `return-from`s on a hit — an unknown block (`(bl.store:get-block ...)` -> NIL) or a known block not containing the tx both fall through to the txindex branch at :79-93, which returns the tx from whatever block the txindex names, with that block's "blockhash" and no "in_active_chain" key. `grep -rn "Block hash not found" src/` returns nothing, so the pre-check exists nowhere in the tree (it has not merely moved). Executed on the warm image: make-test-node + one mempool tx, then `(rpc-getrawtransaction node (list txid-hex 0 <64 'a' chars>))` returned the hex "02000000000101111111..." and `(list txid-hex 1 <bogus>)` returned keys (txid hash version size vsize weight locktime vin vout hex) — no "blockhash", no "in_active_chain". Core would have thrown -5 "Block hash not found" before ever touching the mempool. That single run demonstrates both halves at once (mempool probed ahead of the hint; nonexistent blockhash not rejected). I could not additionally exercise the txindex-points-at-a-different-block path because make-test-node has no chain (best-block-hash is "" and get-block-index-entry is NIL), but the fall-through is unambiguous in the control flow. Consequence as stated holds: a caller using the third argument as an "is this tx in THAT block" containment check gets a false positive from both paths. S3 is right — RPC contract divergence, no consensus or DoS reach; note /rest/tx calls this with no blockhash (src/rpc/rest.lisp:115-116) so the REST surface is unaffected.

### `b48227fc` — S3

**FindAndDelete is skipped for an EMPTY signature; Core's pattern for an empty sig is the single byte 0x00 (OP_0), not the**

Core side verified: script.h AppendDataSize takes the `size < OP_PUSHDATA1` branch for size 0 and inserts one 0x00 byte, so `CScript() << vchSig` with an empty vchSig is the 1-byte script [0x00]; FindAndDelete's `if (b.empty()) return nFound` (interpreter.cpp:231) therefore does NOT fire, and interpreter.cpp:329-332 (CHECKSIG) / :1141-1150 (CHECKMULTISIG) turn found>0 into SCRIPT_ERR_SIG_FINDANDDELETE under CONST_SCRIPTCODE. Our side verified: verify-checksig returns (values nil nil) at src/coalton/interop.lisp:2054-2055 before the CONST_SCRIPTCODE block for a zero-length sig, and strip-sigs-from-script-code maps `((zerop siglen) nil)` and skips (interop.lisp:2405). No other site scans for this — grep over src/ finds only interop.lisp:2120 and :2508 for :sig-findanddelete. EXECUTED: with *script-flags* = the full bl.val:+standard-script-verify-flags+ (which contains CONST_SCRIPTCODE), scriptPubKey `00 21 <33-byte pk> ac` gives (NIL :EVAL-FALSE) where Core gives SIG_FINDANDDELETE; and the reachable relay case — P2SH redeemScript `OP_0 <pk> OP_CHECKSIG OP_NOT` (`00 21 <pk> ac 91`), scriptSig = its canonical push, standard P2SH scriptPubKey — returns (T NIL) for us under the full standard set. Core rejects that spend as non-standard (SIG_FINDANDDELETE). So the stated relay impact is reachable and demonstrated. Severity stays S3: CONST_SCRIPTCODE is absent from our block flags (compute-script-flags-for-height / +always-on-block-script-flags+ = P2SH,WITNESS,TAPROOT plus height-gated DERSIG/CLTV/CSV/NULLDUMMY, src/validation/block.lisp:288-332) and from Core's MANDATORY set, and an empty sig forces CHECK(MULTI)SIG false in both trees regardless of the mutated scriptCode, so there is no consensus split.

### `ba446c02` — S3

**Block script validation never consults the script-execution cache, so every mempool-verified transaction is re-interpret**

CONFIRMED, executed. Read: src/validation/block.lisp:793-849 `validate-tx-scripts` goes collect-spent-utxos -> validate-input-script per input; grep for `script-execution-cache*` over src/ hits ONLY src/coalton/interop.lisp:681-734 (the cache itself) and src/validation/transaction.lisp:1376-1400 (the mempool path). Neither validate-block-scripts (block.lisp:1018) nor validate-block-scripts-parallel (block.lisp:988) nor prefetch-block-spent-coins touches it, so the check has not merely MOVED. Core side verified verbatim: refs/bitcoin/src/validation.cpp:2076-2081 is the `m_script_execution_cache.contains(hashCacheEntry, !cacheFullScriptStore) -> return true` probe, and ConnectBlock's `bool fCacheResults = fJustCheck; /* Don't cache results if we're actually connecting blocks (still consult the cache, though) */` sits in the `if (!tx.IsCoinBase() && fScriptChecks)` block at ~validation.cpp:2571 (the finding cites 2580-2583; same block, line number off by a handful).

EXECUTED PROOF (scripts/dev.sh eval): built a 2-tx block whose spending tx has an empty scriptSig against a P2PKH coin, computed the block's own flags via block-script-flags(header-hash, 0) = "P2SH,TAPROOT,WITNESS", pre-stored make-script-execution-cache-key(wtxid, those flags), then ran both paths on the SAME tx and SAME flags:
  (:FLAGS "P2SH,TAPROOT,WITNESS" :BLOCK-PATH-RESULT (NIL :SCRIPT-FAILED) :TX-PATH-RESULT (T))
The tx path returns T straight out of the cache; the block path re-interprets and rejects. That is the divergence exactly as claimed, and the docstring claim at transaction.lisp:1381-1385 ("the confirming block verifies again, so this turns the second pass into ONE lookup") is indeed not honoured by the block path.

CONSEQUENCE: performance only, and correctness-neutral in both directions — a cache entry means "all inputs succeeded under these exact flags", so consulting it cannot accept anything, and not consulting it cannot reject anything. The block path also never STORES, which is strictly more conservative than Core (Core stores when fJustCheck). Redundant work is interpreter + sighash, not ECDSA (cached-verify-ecdsa / cached-verify-schnorr, interop.lisp:643-664, still absorb the crypto). Severity S3 stands; no upgrade.

### `c831e584` — S2

**The RPC coins sync advances the persisted coins best-block pointer without persisting the block index, so a later crash **

MECHANISM CONFIRMED BY EXECUTION. coins-view-cache-sync (src/storage/coins-view-cache.lisp:336-374) stages (coins-view-batch-set-best-block batch best-block) with best-block defaulting to cvc-best-block; it is called by %coin-view-iterate (coins-view-cache.lisp:896), which is the coins-view-cache arm of utxo-set-iterate (:953) and therefore the path under gettxoutsetinfo/compute-utxo-set-hash (src/rpc/blockchain.lisp:1799), dumptxoutset (:1170) and scantxoutset (:1535). Executed probe: opened a fresh coins-view-db, set cvc-best-block to aa00..00 and called coins-view-cache-sync → the persisted pointer went from NIL to "aa00...00" with no header-index write anywhere on that path. cvc-best-block is advanced per connected block (coins-view-cache.lisp:802) and per disconnect (:852), so it runs ahead of disk between flushes. ORDERING DIVERGENCE IS REAL: our only correct ordering is %flush-chainstate (src/node/flush.lisp:243-277) — Phase 1 save-state + save-header-index, THEN Phase 2 coins flush with the pointer — matching Core's FlushStateToDisk, which does FlushChainstateBlockFile then WriteBlockIndexDB then CoinsTip().Sync() (refs/bitcoin/src/validation.cpp:2777-2812), reached from gettxoutsetinfo's ForceFlushStateToDisk (rpc/blockchain.cpp:1075, validation.cpp:2838-2844). Grepped save-header-index: exactly three call sites (chain.lisp:1085 definition; flush.lisp:250; init.lisp:85 and :630 — both startup/reindex), and %append-header-index-delta has no other caller, so block-index entries created since the last flush are memory-only for up to +flush-every-n-seconds+ = 600 s / 25000 blocks (flush.lisp:3-12, 337-339). CONSEQUENCE HOLDS: reconcile-coins-db-best-block returns :unresolvable when (get-block-index-entry chainstate recorded) is NIL (src/node/recovery.lisp:38-46) and init.lisp:812-815 turns that into init-error "Refusing to start ... Recover by reindexing". The LevelDB write is un-synced, which is still durable across a process crash/OOM-kill (the stated trigger). S2 stands; it needs an RPC plus an unclean shutdown, and is recoverable by -reindex, so not S1.

### `cc698623` — S3

**No REST endpoint checks RPC warmup, so /rest/* serves a half-initialized node during startup instead of 503**

CONFIRMED, executed. Static: `grep -rn warmup src/` finds *rpc-warmup-status* read in exactly ONE place, dispatch-rpc-method (src/rpc/server.lisp:68-78, check is first, no exemptions). src/rpc/rest.lisp contains no reference to warmup at all; rest-handle (rest.lisp:538) routes straight to %rest-* which call the rpc-* FUNCTIONS (e.g. rest.lisp:85 `(rpc-getblockchaininfo node nil)`), bypassing dispatch entirely. The acceptor-level gate (rpc-acceptor acceptor-dispatch-request, server.lisp:1021) enforces only the -rpcallowip ACL. Core: rest.cpp:170-176 CheckWarmup + `if (!CheckWarmup(req)) return false;` at the head of every handler (verified at rest.cpp:182 rest_headers and rest.cpp:394 rest_block).
Reachability verified: start-rpc-server sets warmup (server.lisp:1187-1188, from rpc-config.lisp :warmup "Loading...") and installs the REST surface dispatcher LAST in the same call (server.lisp:1305-1320); %start-rpc-early runs at src/node/init.lisp:774, BEFORE "Replaying mempool..." (init.lisp:780) and the index catch-ups (indexes.lisp:285/320/337/366); finish-rpc-warmup only at init.lisp:1237.
EXECUTED (scripts/dev.sh eval, warm image): with *rpc-warmup-status* bound to "Replaying mempool..." on a test node ->
  (:RPC-PATH (-28 "Replaying mempool...") :REST-MEMPOOL 200 :REST-HEALTH 503)
and /rest/chaininfo.json returned (:STATUS 200 :CTYPE "application/json" :BODY "{\"chain\":\"test\",\"blocks\":0,..."). So the JSON-RPC path answers -28 while REST answers 200 with content, under the identical binding. (/rest/health's 503 is the unrelated bitcoin-lisp tip-liveness probe, rest.lisp:321-337, not a warmup check.) Aggravating: init.lisp:762-772's own comment asserts "Every request answers RPC_IN_WARMUP (-28) with the current status until start-node finishes" — false for /rest/. No comment anywhere documents a deliberate divergence. S3 stands: client-visible availability/semantics only (and /rest/ is off by default in both trees, as is Core's), no consensus or security consequence.

### `ccac3137` — S3

**The intra-block coin overlay keeps provably-unspendable outputs that Core's AddCoin drops**

MECHANISM CONFIRMED, executed; CONSEQUENCE only partly follows. Read: src/validation/block.lisp:1757-1765 (coinbase outputs) and ~1800-1806 (per-tx outputs) stage EVERY output into pending-utxos with no filter, while src/storage/coins-view-cache.lisp:563, 645 and 786 all apply `unless (script-unspendable-p spk)` on real application. script-unspendable-p (src/storage/utxo.lisp:19-27) is Core's IsUnspendable (leading 0x6a or len > 10000). Core side confirmed: AddCoin's `if (coin.out.scriptPubKey.IsUnspendable()) return;`, reached from UpdateCoins at validation.cpp:2589, and CheckTxInputs' HaveInputs -> bad-txns-inputs-missingorspent.

EXECUTED PROOF 1 (scripts/dev.sh eval): staged an OP_RETURN output (value 5,000,000) into a pending-utxos table exactly as %contextual-check-block does, then ran validate-transaction-contextual on a tx spending it:
  (:IS-UNSPENDABLE T :CONTEXTUAL-OK T :CONTEXTUAL-ERR NIL :FEE-CREDITED 4999999)
Core would have returned bad-txns-inputs-missingorspent here. So the input resolves and its value is credited as fee.

EXECUTED PROOF 2: same block run through validate-block-scripts with extra-coins = pending, for both unspendable kinds:
  (:OP-RETURN (NIL :SCRIPT-FAILED) :OVERSIZE (NIL :SCRIPT-FAILED))
So with scripts ON the block is still rejected, just with the wrong reason and after full script work — exactly as the finding concedes.

CONSEQUENCE CORRECTION: the sharp half of the impact ("when script checks are skipped nothing catches it") is NOT reachable. skip-scripts comes solely from script-checks-skippable-p (block.lisp:1311-1359), which after the GA9 S2-2 fix requires assumevalid to be configured, its header to be in the index, and THIS BLOCK'S HASH to equal the assumevalid chain's ancestor at that height. An attacker cannot mint such a block, and every real ancestor of a hardcoded assumevalid hash was accepted by Core and therefore contains no same-block spend of an unspendable output. So the divergence is a latent hazard plus a wrong reject reason, not a live inflation path. Severity S3 stands.

### `d2099b36` — S2

**The "-peerblockfilters without -blockfilterindex" refusal is nested inside (when prune ...), so an unpruned node adverti**

Read + EXECUTED. src/node/init.lisp:239 opens `(when prune`; the peerblockfilters config-error at :253-254 is a subform of it, sandwiched between the txospenderindex check (:250-251) and the reindex-chainstate check (:255-256). Executed in the warm image (globals rebound so nothing leaks): `(%init-parameters :testnet4 nil nil nil nil nil nil nil nil nil t nil)` => :STARTED-OK; the identical call with prune=550 => "ERROR: Cannot set -peerblockfilters without -blockfilterindex." So the refusal exists ONLY under -prune, exactly as claimed. Core refs/bitcoin/src/init.cpp:992-999 runs the check unconditionally and BEFORE the `if (args.GetIntArg("-prune", 0))` block at :1001-1008 (verified by sed). Consequence executed as well: `(let ((bl:*peer-block-filters* t)) (local-services))` = 1097, and (logtest 1097 +node-compact-filters+=64) => T, i.e. NODE_COMPACT_FILTERS is advertised (src/networking/peer.lisp:909-911). Serving is then dead: %cf-serving-index (src/networking/protocol.lisp:2148-2154) requires (blockfilterindex-enabled bfi), and handle-getcfilters/getcfheaders/getcfcheckpt each wrap their whole body in `(when bfi ...)` (:2180, :2201, :2231) so the request is dropped with no reply and no disconnect. Checked for a MOVED check: grep -rn "peerblockfilters|peer-block-filters" over src/ shows the only gate is init.lisp:253; grep for "blockfilterindex" shows nothing enables the index implicitly when -peerblockfilters is set. Reachability confirmed end-to-end: (args->start-node-plist '("-testnet4" "-peerblockfilters=1")) => (:PEER-BLOCK-FILTERS T ...). The docstring at src/node/init.lisp:835 ("gated in %INIT-PARAMETERS") is true only under -prune. Mechanism and consequence both hold; S2 unchanged.

### `d410b7bf` — S3

**script-is-push-only-p rejects OP_RESERVED (0x50); Core's IsPushOnly deliberately counts it as push-type**

MECHANISM CONFIRMED by execution. `./scripts/dev.sh eval '(list (bitcoin-lisp.coalton.interop::script-is-push-only-p #(#x50)) (... #(#x51)) (... #(#x61)) (bitcoin-lisp.validation::scriptsig-push-only-p #(#x50)))'` -> (NIL T NIL T). So interop's predicate rejects OP_RESERVED while Core's IsPushOnly (script.cpp:266-281, `if (opcode > OP_16) return false;` with the explicit comment that OP_RESERVED IS push-type) accepts it. src/coalton/interop.lisp:1353-1355 is the `((= op #x50) (return-from ... nil))` clause; it is reached from verify-script at :823 (SIGPUSHONLY) and :831 (P2SH).

End-to-end: with scriptSig = #(#x50 #x01 #x51) and scriptPubKey = P2SH(OP_1), `verify-script` returns (NIL :SIG-PUSHONLY) under flags "P2SH" and under "P2SH,SIGPUSHONLY"; with no flags it returns (NIL :ERROR) (engine executes OP_RESERVED and fails). Core would return BAD_OPCODE on both paths: interpreter.cpp:2012 runs the SIGPUSHONLY IsPushOnly check first but it passes (OP_RESERVED <= OP_16), then EvalScript(scriptSig) hits the switch default; interpreter.cpp:2055's P2SH IsPushOnly is only reached after EvalScript succeeded, which it cannot.

CONSEQUENCE as filed is accurate and bounded: reject in both trees, only the error keyword differs (:sig-pushonly vs BAD_OPCODE). No path where we reject something Core accepts -- OP_RESERVED at scriptSig top level always has fExec=true, and any OP_IF that could make it unexecuted is itself > OP_16 and non-push in both trees.

I checked the "moved elsewhere" hypothesis and it does NOT rescue this call site, though it does confine the blast radius: the POLICY predicate src/validation/transaction.lisp:410 `scriptsig-push-only-p` uses `(> op #x60)` and matches Core (returns T for #x50, verified above), and the SIGOP predicate src/validation/block.lisp `p2sh-sigop-subscript` documents OP_RESERVED as push-type in its docstring. Only interop's consensus-path copy diverges.

No Core script_tests.json vector covers this shape (the one at line 1139, "OP_RESERVED in P2SH should fail", has a push-only scriptSig `0 0x01 0x50` -- OP_RESERVED is the redeem script, not a scriptSig opcode), so the suite cannot catch it. S3 stands: real divergence, error-code only.

### `d4f123e8` — S2

**A negated repeatable option (-noX) is appended to the list as the literal string "0" instead of clearing it; -norpcauth **

CONFIRMED, all four sub-claims executed. Mechanism: src/config/args.lisp:40-56 interpret-arg maps -noKEY to (KEY, "0", json false) unconditionally; parse-cli-args (args.lisp:57-85) keeps EVERY occurrence when config-option-repeatable-p, and src/node/args.lisp:33-39 collects every merged-alist value verbatim. grep for 'negat' across src/config/ and src/node/ finds only the -no prefix stripping and the double-negative warning — there is no negation-clears-the-span rule anywhere, so the check has not moved. Core common/settings.cpp:203-246 + SettingsSpan::begin()=data+negated() (:264-274) erases the span up to the last false, sets done, and prev_negated_empty (:243) suppresses the config-file zombies; GetArgs (args.cpp:367-374) therefore yields an EMPTY list. EXECUTED in the warm image: (a) '-rpcauth=u1:aa$bb -rpcauth=u2:cc$dd -norpcauth' -> :RPC-AUTH ("u1:aa$bb" "u2:cc$dd" "0"); parse-rpcauth-entry "0" => NIL and %parse-rpcauth-credentials => :INVALID, and start-rpc-server returns NIL at src/rpc/server.lisp:1197-1198 while src/node/rpc-config.lisp:106-123 just carries the NIL server on — the node comes up with NO RPC, where Core's rpc_users.py:178-181 expects a live server answering 401. (b) '-onlynet=ipv4 -noonlynet' -> apply-parameter-interactions signals "Unknown network specified in -onlynet: \"0\"" (src/config/values.lisp:229) = startup failure on a command line Core accepts. (c) '-nodebugexclude' -> exclude ("0") and apply-log-categories signals "Unsupported logging category -debugexclude=0." (src/logging.lisp:446). (d) with the functional framework's conf (test_framework.py:127 disable_autoconnect=True writes connect=0, util.py:580-581), (args->start-node-plist '("-noconnect" "-chain=regtest") '("connect=0")) gives :CONNECT-NODES ("0" "0"), which is NOT `equal` '("0") at src/node/init.lisp:406, so *connect-nodes* becomes ("0" "0") and we log 'Connecting only to -connect peers' and dial a host named "0"; and with a real peer, '-noconnect' + 'connect=10.0.0.5' gives ("0" "10.0.0.5") — the operator's negation does not clear the configured peer, which Core's prev_negated_empty gate does. S2 stands.

### `d73be57b` — S3

**Rolling minimum fee decays with no block since the bump, and block connection never resets the decay clock**

CONFIRMED, executed. Both mechanisms are real and both consequences are reachable.

(a) No block-since flag. Core txmempool.cpp:853-858 trackPackageRemoved sets `blockSinceLastRollingFeeBump = false`, GetMinFee at :830-831 returns the UNDECAYED rate while that flag is false, and removeForBlock at :426-427 sets `lastRollingFeeUpdate = GetTime(); blockSinceLastRollingFeeBump = true`. `grep -rn 'rolling-min-fee-time|rolling-min-fee-rate|block-since' src/` finds only the two slots (src/mempool/mempool.lisp:372-373), the read in mempool-decayed-rolling-min-fee-rate (:474-489) and the single write in mempool-trim-to-size (:1721-1724). No block-since slot exists, and mempool-remove-for-block (:1756-1793) never touches rolling-min-fee-time.

(b) Retroactive half-life. Ours recomputes `rolling * 0.5^(age/halflife)` from the original anchor with the half-life chosen from usage AT READ TIME and never writes the result back (only the reset-to-0 branch writes, :487). Core mutates rollingMinimumFeeRate/lastRollingFeeUpdate in place each call (:840-841), so its decay is piecewise in the half-life that was actually in force.

Executed, empty pool (usage 0 < limit/4 -> 3 h half-life), rolling bumped to 10000 sat/kvB at t=0, no block ever connected:
  (:USAGE 0 :LIMIT 300000000 :AT-0S 10000 :AT-600S 9622 :AT-12H 625)
Core returns a flat 10000 for the whole span. 3.8 % low after one block interval, 16x low after 12 h.

Reachable: mempool-effective-min-fee-rate (mempool.lisp:450-459) feeds tx admission (src/validation/transaction.lisp:1251), package validation (src/validation/packages.lisp:474), getmempoolinfo (src/rpc/mempool.lisp:69) and the BIP133 feefilter (src/networking/peer.lisp:1331). Effects are bounded to the congested case (rolling only exceeds minrelaytxfee after a real trim) and the size cap still bounds memory since trimming re-bumps, so this is a policy-only loosening: S3 stands.

### `d8cffd5c` — S3

**PSBT signing never checks that existing signatures use the requested sighash type**

CONFIRMED by reading both trees; the check has not moved. Core refs/bitcoin/src/psbt.cpp:459-475 ("Check all existing signatures use the sighash type") returns PSBTError::SIGHASH_MISMATCH when, for SIGHASH_DEFAULT, m_tap_key_sig or any m_tap_script_sigs entry is not exactly 64 bytes, and otherwise when any tap sig is not 65 bytes ending in the sighash byte or any partial_sigs entry's last byte differs. Our %psbt-effective-sighash (src/wallet/psbt.lisp:1073-1086) ports only the PSBT_IN_SIGHASH-vs-parameter comparison at :1081-1083 ("Specified sighash and sighash in PSBT do not match."), and %psbt-record-signatures (:1249-1264) goes straight from that to %compute-input-signatures and records our sig next to whatever was there. grep over src/wallet/ and src/rpc/ finds exactly one occurrence of the mismatch string (psbt.lisp:1083) and no length/last-byte inspection of +psbt-in-partial-sig+ / +psbt-in-tap-key-sig+ / +psbt-in-tap-script-sig+ anywhere; the finalizer takes sigs purely by pubkey (%psbt-multisig-sigs, :~700) so it does not catch it later either. Consequence judged separately and it is narrower than a funds risk: our own signature still commits with SIGHASH_ALL, so the assembled tx cannot be mutated behind us — what is lost is Core's "For user safety" abort, i.e. the only signal that a co-signer committed to different fields. Missing hardening / lost warning: S3 stands.

### `d9aadbc5` — S2

**IsBadPort is absent: automatic outbound dials will connect to any gossiped port (SMTP, SSH, MySQL, IRC, …)**

CONFIRMED, and reproduced by execution. Nothing in the tree implements a port deny-list: `scripts/dev.sh eval '(list (apropos-list "BAD-PORT") (apropos-list "BADPORT") (apropos-list "PORT-ALLOW"))'` returned (NIL NIL NIL) against the loaded image, and grep -rni for badport/bad_port/p2p-bad-ports/forbidden.port/denied.port over src/ and tests/ hits only docs/gap-analysis-10.md (this same finding). The port is never inspected on any dial path: select-dialable-address (addrman.lisp:535-550) filters only dialable-network-p + reachable-network-p; %addrman-pick-unconnected (peers.lisp:885-901) returns the stored port verbatim; establish-outbound-peer (peers.lisp:773-801) hands it straight to connect-peer; connect-peer (peer.lisp:296-313) checks only ban/discourage; make-tcp-connection (connection.lisp:228) checks only the proxy; %ingest-gossiped-address (protocol.lisp:1208-1253) and address-book-add (addrman.lisp:386-430) store whatever port arrives.

EXECUTED: added a gossiped record 198.51.100.7 port 25 to a fresh address book and drew from the mandated automatic-dial filter ->
  (:COUNT 1 :SELECTED (("198.51.100.7" 25)))
so an SMTP port survives the one filter every automatic path (outbound refill, block-relay slots, feelers) is required to go through, and connect-to-peers copies that stored port into node-known-addresses as the dial port (peers.lisp:337-341).

core_ref verified verbatim: refs/bitcoin/src/net.cpp:2854 `if (nTries < 50 && (addr.IsIPv4() || addr.IsIPv6()) && IsBadPort(addr.GetPort())) continue;`, IsBadPort at refs/bitcoin/src/netbase.cpp:847-935 with the 'Don't forget to update doc/p2p-bad-ports.md' comment, plus init.cpp:2135/2169. Impact is reachable as stated (we dial and speak the P2P protocol at an arbitrary third-party service, and waste automatic outbound slots). One overstatement worth noting: the 're-gossip to two more nodes' half is NOT a divergence -- Core also stores and relays bad-port addresses; only the automatic DIAL is filtered. That does not change the severity: the dial itself is the abuse vector doc/p2p-bad-ports.md exists for. S2 stands.

### `df63423d` — S3

**Gossiped addresses are stored in addrman and relayed onward without the banned/discouraged filter Core applies at ingest**

MECHANISM CONFIRMED BY EXECUTION. Core net_processing.cpp:4092-4097: after AddAddressKnown, `if (m_banman && (m_banman->IsDiscouraged(addr) || m_banman->IsBanned(addr))) continue;` — the continue skips both RelayAddress (:4098-4101) and the vAddrOk push feeding m_addrman.Add (:4103-4105). Ours, %INGEST-GOSSIPED-ADDRESS (src/networking/protocol.lisp:1226-1258), gates only on MAY-HAVE-USEFUL-ADDRESS-DB-P, REACHABLE-NETWORK-P and the freshness/routability relay test; ADDRESS-BOOK-ADD at :1251, relay cons at :1256-1258. Grepped for a filter that MOVED: the only ban/discourage call sites in src/ are %SAMPLE-ADDR-RESPONSE (protocol.lisp:2486-2489, getaddr cache FILL) and CONNECT-PEER (peer.lisp:299-300, dial time); the caller %PROCESS-GOSSIPED-ADDRESSES (protocol.lisp:1290-1340) adds none either. Executed the end-to-end path: discouraged 8.8.4.4, then %INGEST-GOSSIPED-ADDRESS on a net-addr for it -> (:DISCOURAGED T :STORED 1 :RELAY-CANDIDATE T :RELAY-TARGETS 2). So we do store it in addrman and do hand it to RELAY-ADDRESS with a 2-peer fan-out. CONSEQUENCE HOLDS but is minor in magnitude: our bans are manual-only (setban -> *banned-peers*) and discouragement is a bounded rolling filter, so the poisoned share of addrman is small, and the getaddr-cache filter already blocks the pull path. Missing-hardening class, no DoS or consensus reach = S3.

### `e2db2089` — S2

**-rpcwhitelist / -rpcwhitelistdefault are accepted at startup but never enforced, so a user the operator restricted gets **

Mechanism confirmed by execution and by grep. `./scripts/dev.sh eval` returns `(T ("rpcwhitelist") NIL)` for, respectively, (a) `bitcoin-lisp.config::core-only-option-p "rpcwhitelist"` = T, (b) `supplied-core-only-options` reporting it as accepted-but-ignored, and (c) `(apropos-list "WHITELIST" (find-package "BITCOIN-LISP.RPC"))` = NIL — there is no whitelist symbol of any kind in the RPC package. A case-insensitive grep for "whitelist" across src/ finds only the P2P net-permission machinery (src/networking/netaddress.lisp:668-728, src/config-options.lisp:148-149,361-363), the core-only registration at src/config-options.lisp:404, and the dangling docstring at src/rpc/server.lisp:814. So the check has not MOVED; it does not exist. The call site confirms the username is discarded: src/rpc/server.lisp:901 is `(unless (check-auth auth-header) ...)` — the returned user name is never bound, and dispatch-rpc-method (src/rpc/server.lisp:68-85) looks the method up in *rpc-methods* with no per-user filter. Core does gate: httprpc.cpp:145-158 (403 for a whitelist-less user when g_rpc_whitelist_default, 403 for an off-whitelist method) and :176-189 (pre-checks every batch member), with g_rpc_whitelist_default defaulting to true whenever any -rpcwhitelist is given (httprpc.cpp:306). Since -rpcauth IS implemented here (*rpc-credentials*), the stated recipe is reachable and the control fails open. Mitigation the finding already prices in and which I verified is real and reaches debug.log: src/node/init.lisp:1573-1577 emits, via defer-log :warn flushed at src/node/init.lisp:151, "Accepted but NOT implemented by this node, so this option has no effect: -rpcwhitelist". That makes it an announced unimplemented feature rather than a silent one, but it does not change what the running node serves, so I leave the severity at S2 rather than downgrading: a restricted credential still gets stop/sendrawtransaction/setban/wallet where Core answers 403.

### `f506ca73` — S3

**No -walletcrosschain guard: a wallet whose locator belongs to another chain is loaded and rewritten**

CONFIRMED as a missing guard, with the reachability narrower than the write-up implies. Core wallet.cpp:3178-3190 refuses AttachChain when locator.vHave.back() != chain.getBlockHash(0) unless -walletcrosschain. Our wallet-attach-chain (src/wallet/wallet-tx.lisp:1650-1673) uses the stored locator only for find-fork-in-active-chain and falls back to rescan-height 0; grep for crosschain/genesis over src/wallet/ finds no genesis comparison, and the wallet's :network slot is taken from the manager at load (wallet.lisp:997, :1243), never read back from the file. -walletcrosschain itself is registered as accepted-and-ignored (src/config-options.lisp:409 define-core-only-options), so we behave as if it were permanently on. Downstream mechanics check out: %wtx-update-state-from-chain (wallet-tx.lisp:1222-1233) demotes :confirmed to :inactive when the block is not on the active chain, and wallet-post-load-resubmit (wallet-spend.lisp:2587) then pushes those txs at the mempool. Two corrections to the impact, which is why I leave it S3: (1) the restorewallet route is ALREADY guarded — %parse-wallet-dump (wallet-crypt.lisp) refuses a dump whose "network,<name>" line does not match, with the comment "a mainnet backup must not restore onto a testnet node", so Core's wallet_crosschain.py restore assertions are covered; loadwallet with an absolute path is also refused (%valid-wallet-name-p, wallet.lisp:342-364). (2) the default layout is per-network (node-data-directory = network-data-path, src/node/datadir.lisp:153-180, wallets/ hangs off it), so this needs a deliberately shared -walletdir or a hand-copied wallet directory. Effect is then a wallet that reads as emptied and gets its locator rewritten, recoverable by reopening on the right chain — except on a pruned node, where the birthday rescan can hit the "Prune: last wallet synchronisation goes beyond pruned data" bail in the same function; that conditional case is the only part with teeth.

### `f5b31fb7` — S3

**OP_CHECKMULTISIG charges nKeysCount against the 201-op budget only after running the signature verifications; Core charg**

MECHANISM CONFIRMED by reading and by execution. Core interpreter.cpp:1116-1121 charges and enforces immediately after the nKeysCount range check and BEFORE FindAndDelete and the verification while-loop. Ours: src/coalton/script.lisp:1975 calls DO-CHECKMULTISIG-STACK-OP (src/coalton/interop.lisp:2425-2515+, which pops everything, runs strip-sigs-from-script-code and verify-checkmultisig-for-script) and only then, at script.lisp:1983/1990 (CHECKMULTISIG) and :2031 (CHECKMULTISIGVERIFY), computes `(+ (context-op-count ctx) n-pubkeys)` and compares to +max-ops-per-script+. `grep -n 'max-ops-per-script|op-count' src/coalton/script.lisp` shows the only nKeysCount charge sites are 1983/1990/2031 -- there is no earlier pre-charge anywhere, so "it moved" does not apply.

Ordering demonstrated live: executing `<N x OP_NOP> 0x01 0x14 OP_CHECKMULTISIG` via bitcoin-lisp.coalton.script:execute-script gives SE-STACKUNDERFLOW at N=100/181/182 and SE-TOOMANYOPS only at N=201. At N=182 Core returns SCRIPT_ERR_OP_COUNT (182 NOPs + 1 for the CHECKMULTISIG opcode + 20 keys = 203 > 201) without touching the stack; we instead run the multisig and report underflow. The N=201 TOOMANYOPS comes from the generic per-opcode counter (script.lisp:2253-2256), not the nKeysCount charge, which confirms the key charge is post-hoc.

CONSEQUENCE: verdict-identical (every path that skips the charge already returns an error, and the over-budget case still returns SE-TooManyOps), so this is resource-only, as filed. But the filed "cheap DoS amplification" overstates it: the extra work is at most one CHECKMULTISIG's worth of ECDSA verifications on the single script that busts the budget (the ScriptErr aborts execution, so no second one runs), capped at MAX_PUBKEYS_PER_MULTISIG = 20 and only reachable with 1 sig / 20 keys -- with 20 sigs the `nSigsCount > nKeysCount` early-exit fires after one check. An attacker can already legitimately demand ~200 verifications from an IN-budget script that Core also performs, so the amplification factor over what is freely available is ~1.1x, on top of unchanged size and sigop-limit costs. Real divergence, negligible consequence: S3 stands, no upgrade.

### `fac08286` — S3

**Coinbase "hash" (wtxid) reported as 32 zero bytes by the RPC transaction serializer; Core reports the real witness hash**

CONFIRMED, executed. I built a coinbase transaction with a witness in the warm image and asked both paths: (:HAS-WITNESS T :COINBASE-P T :OUR-WTXID "0000...0000" :CORE-WITNESS-HASH "1e379a24db37a89045b4f3be3d7933fc1f17d86b4649140211f05035c69a58e8" :RPC-HASH-FIELD "0000...0000" :RPC-TXID-FIELD "628aab1a..."). So bl.rpc::tx-to-json's "hash" field (/Users/sen/common-lisp/bitcoin-lisp/src/rpc/blockchain.lisp:466) is literally 32 zero bytes for a coinbase, from transaction-wtxid's coinbase special case at src/serialization/types.lisp:443-446. Core side verified: primitives/transaction.cpp:85-93 ComputeWitnessHash has no coinbase branch (txid when !HasWitness, else hash256 of the witness serialization), the zero convention lives ONLY in BlockWitnessMerkleRoot (consensus/merkle.cpp:80 `leaves.emplace_back(); // The witness hash of the coinbase is 0`), and core_io.cpp:435 emits `entry.pushKV("hash", tx.GetWitnessHash().GetHex())`. Reachable from getblock verbosity 2/3 (blockchain.lisp:394 loops tx-to-json over every tx incl. the coinbase) and getrawtransaction (rawtransaction.lisp:19/54/151). Note it is wrong for pre-segwit coinbases too: Core would report the txid there, we still report zeros. The consensus use is correctly unaffected — compute-witness-merkle-root (src/validation/block.lisp:1091-1095) depends on the zero, and the wallet already carries a private workaround (%tx-witness-hash, src/wallet/wallet-tx.lisp:87-96) that the general RPC path does not use. Severity unchanged: read-only RPC data fidelity, no consensus, wallet, or relay effect.

### `fdbe8a5e` — S3

**The prune window's lower bound is our walk cursor rather than Core's 0, so the first blk file is never prunable**

Mechanism CONFIRMED, executed. The stated disk-overrun impact does NOT follow; the real consequence is milder.

src/storage/blocks.lisp:889 calls `(%prunable-flat-files store (1+ start) min-keep-height)` where start = chain-state-prune-walk-start = `(max pruned-height prune-floor)` (src/storage/chain.lisp:201-207), and both default to 0 (chain.lisp:57 `pruned-height 0`; chain.lisp:199 floor 0 for a non-snapshot chainstate). %prunable-flat-files (blocks.lisp:743-758) requires `(>= first min-height)`, so with min-height 1 the file holding genesis (height-first 0, written by ensure-genesis-on-disk, blocks.lisp:624) is excluded forever, and the floor only rises. Core's GetPruneRange (validation.cpp:6366-6379) returns prune_start=0 for an ordinary chainstate and blockstorage.cpp:386 tests `nHeightFirst < min_block_to_prune` i.e. `0 < 0` = false, so file 0 is prunable.

Executed (fabricated file-info, no I/O):
  %prunable-flat-files with file0=[0,99], file1=[100,199]
  => (:AUTO-FLOOR-1 (1) :CORE-FLOOR-0 (0 1))
The automatic path's floor of 1 drops file 0; the manual path already uses Core's floor — blocks.lisp:961 passes 0 — so pruneblockchain (the only caller, src/rpc/blockchain.lisp:2068) can remove it while automatic pruning never does. That asymmetry inside our own tree is itself evidence the (1+ start) is unintended.

Severity correction on the IMPACT: 'actual disk use exceeds the target by that amount for the life of the datadir' is wrong. prune-old-blocks re-checks `(<= (block-store-total-bytes store) target-bytes)` at the top of every file iteration (blocks.lisp:890) and total-bytes includes file 0, so the loop simply prunes further, newer files until the total is back under target. Net effect: blk00000.dat+rev00000.dat are retained permanently and an equal volume of NEWER history is pruned in their place — the retained window is shifted, not the footprint. Disk only exceeds the target when the whole window is exhausted, which is the ordinary MIN_BLOCKS_TO_KEEP behaviour and not caused by this bug. Real Core divergence, no practical harm: S3 stands.

### `feac3eb3` — S2

**/rest/spenttxouts serializes the on-disk compressed CBlockUndo codec instead of Core's REST format, and its JSON omits C**

Confirmed on both sides, with the byte string produced by execution. Core's rest.cpp:277-289 SerializeBlockUndo is REST-specific: WriteCompactSize(vtxundo.size()+1), WriteCompactSize(0) as the coinbase placeholder, then per tx WriteCompactSize(vprevout.size()) and `coin.out.Serialize(stream)`, which is CTxOut's SERIALIZE_METHODS at primitives/transaction.h:151 — raw int64 nValue plus a CompactSize-prefixed script, no height, no coinbase flag, no compression. BlockUndoToJSON (rest.cpp:293-296) pushes an empty VARR first. Ours: src/rpc/rest.lisp:506 calls bl.store:serialize-block-undo, which is the disk codec (src/storage/block-undo.lisp:58-67): `bb-write-varint` (CompactSize, src/util/bytes.lisp:160) of (length tx-undos) with NO +1 and NO leading zero, then per coin bb-write-undo-coin = core VARINT(height*2+coinbase), a 0x00 dummy when height>0, then bb-write-compressed-tx-out. Executed: serializing one tx-undo holding one coin (value 5000000000, height 100, coinbase, 26-byte script) yields `0101804900322076a914...` — outer count 01 (Core: 02 00), then VARINT 0x8049 = 201 = height*2+1, dummy 00, compressed amount 0x32 = 50, script size byte 0x20 = 26+6. Core's bytes for the same undo would be `02 00 01` + `00f2052a01000000` + `1a` + script. Nothing in this tree corrects it: the only tests touching the endpoint (tests/rpc/rpc-tests.lisp:3283, 3319) assert only that it routes and rejects a bad hash, never the byte or JSON shape, and %block-undo-json (src/rpc/rest.lisp:513-537) is a bare mapcar over the non-coinbase tx-undos with no leading empty array. Both consequences follow: the .bin/.hex form is undecodable by a Core-written client, and the .json arrays are shifted one position, attributing tx i+1's prevouts to index i.

### `ff51e9d5` — S3

**Inbound admission ban/discourage checks have no NoBan permission exemption**

MECHANISM CONFIRMED, executed. src/node/eviction.lisp:284-312 `inbound-connection-allowed-p` is a 4-way COND on (peer-banned-p host) / pending-queue backlog / (peer-discouraged-p host) — it never calls bl.net:peer-permission-flags. src/node/listen.lisp:20-32 is the only caller and passes only (connection-host conn), so no permission lookup happens upstream either. grep for +perm-noban+ across src/ shows it used in protocol.lisp:219, peer.lisp:1652 (record-misbehavior) and eviction.lisp:190 (ProtectNoBanConnections) — i.e. the flag exists and is consulted everywhere EXCEPT accept-time admission. Core net.cpp:1798-1812 guards both drops with `!NetPermissions::HasFlag(permission_flags, NetPermissionFlags::NoBan)`, the flags having been filled from vWhitelistedRangeIncoming at net.cpp:1771. EXECUTED: with *whitelist-entries* = (parse-whitelist-entry "noban@198.51.100.9/32") and ban-address on that host, the image returns (:NOBAN-P T :BANNED T :ADMISSION (NIL :BANNED)) — the noban peer is refused where Core admits it. CONSEQUENCE PARTLY NARROWED (not enough to refute): the finding's regtest/127.0.0.1-discouraged path is NOT reachable, because record-misbehavior (peer.lisp:1652-1668) already returns early for a noban peer AND skips discourage-peer for any loopback-address-p address, and discourage-peer has no other call site in src/. The reachable path is the ban arm: ban-address (setban RPC) or a banlist.json entry persisted from before the whitelist was configured. Severity S3 stands — operator-configured -whitelist=noban does not survive our own ban list, but it takes an explicit setban (or a stale persisted ban) to get there.

### `refactor-1` — S3

**fsync-parent-directory was written to fix a durability bug and wired into only 3 of its 7 drive sites — 4 atomic-rename **

MECHANISM CONFIRMED by execution; CONSEQUENCE and the "refactor regression" framing both wrong, so this is a severity correction, not a HIGH.

(1) fsync-directory really does sync the FILE when handed a file path. /Users/sen/common-lisp/bitcoin-lisp/src/kv/fsync.lisp:16 does `(sb-posix:open (namestring dir) o-rdonly)` + fsync inside `(handler-case ... (error () nil))`. Executed in the warm image (same open/fstat the defun performs):
  what-fsync-directory-would-open-for-a-file (:ARG "/tmp/fsync-probe-r1/settings.json" :INO 1403137 :DIRP NIL)
  what-it-should-open              (:ARG "/tmp/fsync-probe-r1/"              :INO 1403136 :DIRP T)
  (bitcoin-lisp.kv:fsync-directory <file>) => 0   ; succeeds, silently, on the wrong inode
Also executed: `bl.store::fsync-directory` is EQ to `bitcoin-lisp.kv:fsync-directory` (symbol-package BITCOIN-LISP.KV) — there is no storage-local file-path-taking shadow, so the four sites really do reach the directory-taking function.

(2) The four sites really pass a file. /Users/sen/common-lisp/bitcoin-lisp/src/node/datadir.lisp:249 (path = settings-path from src/node/init.lisp:1555/1593); /Users/sen/common-lisp/bitcoin-lisp/src/wallet/wallet.lisp:1365 (path = (settings-json-path datadir), wallet.lisp:1311 = merge-pathnames "settings.json"); /Users/sen/common-lisp/bitcoin-lisp/src/mempool/mempool.lisp:1589 (%save-bytes-atomically, called at :1602 from save-mempool-file); /Users/sen/common-lisp/bitcoin-lisp/src/wallet/wallet-crypt.lisp:676 (%write-wallet-dump's backup destination). Each sits right after rename-file, under a comment asserting the directory is synced. Only src/kv/flatfile.lisp:281 passes a real directory. So 3-of-7 wiring is accurate.

WHY THE SEVERITY DROPS:
(a) NOT a refactor regression. `git grep -n fsync-directory 5f4f321 -- src/` shows the same four calls, byte-identical, at src/mempool/mempool.lisp:1589, src/node.lisp:4235, src/rpc/wallet-crypt.lisp:676, src/rpc/wallet.lisp:1380. Pre-refactor, src/storage/utxo.lisp:274 defined a file-path fsync-directory but src/storage/flatfile.lisp:284 redefined it in the same package and bitcoin-lisp.asd loaded flatfile AFTER utxo, so the directory-taking one won at runtime and these four already fsynced the file. Behaviour at these sites is unchanged by PR 525; the refactor strictly improved the tree (fixed utxo's three). The finder concedes this in its own text.
(b) NOT a Core divergence. `grep -rn DirectoryCommit refs/bitcoin/src/` returns exactly ONE call site: refs/bitcoin/src/flatfile.cpp:108 — the block/undo allocator, which is the one place we get right. Core's set

### `refactor-2` — S3

**bl:token-bucket and bl:make-token-bucket are now undefined symbols — a real regression from the util-layer split**

CLAIM CORRECT (verified statically at the pre-fix commit). `git grep -n token-bucket 3470694 -- src/`: the only `(defstruct token-bucket)` is 3470694:src/util/ratelimit.lisp:11 in package BITCOIN-LISP.RATELIMIT, which exports all 8 names (src/util/package.lisp:233-241); 3470694:src/package.lisp:23-25 imported only 6 (no #:token-bucket, no #:make-token-bucket) while :106-107 still exported both. So BITCOIN-LISP:TOKEN-BUCKET / :MAKE-TOKEN-BUCKET were fresh symbols naming nothing. The move was 40d8ed5 (PR 551) as the finder says; fb5cbb4's own commit message mislabels it "P6d (PR 551)" (P6d is 3470694/PR 562) — a label slip, not a substantive error. Also: fb5cbb4's message says a caller "got a reader error"; that is wrong (the symbol exists and reads fine — you get undefined-function / unknown-type at compile or run time). The finder's report said "signals", which is right.

CONSEQUENCE: latent only. No in-tree caller used either name (my export sweep below finds no residue), so nothing was ever broken at runtime — hence S3 rather than the finder's MEDIUM. Mechanism real, impact unreachable.

FIX IS COMPLETE (executed, warm image at HEAD ee312af). `(fboundp 'bitcoin-lisp:make-token-bucket)` -> #<FUNCTION BITCOIN-LISP.RATELIMIT:MAKE-TOKEN-BUCKET>; `(find-class 'bitcoin-lisp:token-bucket nil)` -> #<STRUCTURE-CLASS BITCOIN-LISP.RATELIMIT:TOKEN-BUCKET> — the re-export now names the same object the rest of the tree uses. NO OTHER DEAD EXPORTS: I ran the ratchet's guard over ALL 28 BITCOIN-LISP* packages (2,360 external symbols), i.e. wider than the ratchet's own scope, including the Coalton and tests packages it excludes. Exactly one hit: BITCOIN-LISP.COALTON.TESTS:COALTON-TESTS, which is a fiveam suite name (`def-suite coalton-tests`, tests/coalton/coalton-package.lisp:16) — legitimate, and deliberately out of scope. I also checked the more general shape the bug belongs to: across the project's non-Coalton packages there are ZERO exported names that resolve to two different symbol objects, so no other "export list carried over, import list retyped" residue exists.

THE RATCHET REALLY FIRES (positive control by execution, not by reading). `(fiveam:run 'bitcoin-lisp.tests::every-export-names-something)` -> TEST-PASSED on the clean tree. I then created BITCOIN-LISP.PROBE with one dead export in the live image and re-ran the REAL test: TEST-FAILURE, reason "1 exported symbol name nothing ... (\"BITCOIN-LISP.PROBE:DEAD-ONE\")". Deleting the probe package returned it to TEST-PASSED (image left clean; both probe packages deleted and re-verified NIL). So the sweep is not vac

### `refactor-3` — S3

**known-config-option-p silently lost its case-insensitive fallback for core-only options**

MECHANISM CONFIRMED by diff + execution. Pre (git show 5f4f321:src/config.lisp:1159-1170) known-config-option-p's last arm was (core-only-option-p name), and core-only-option-p at :1148 did (member (string-downcase name) *core-only-config-options*). Post (src/config/args.lisp:96-108, moved by bd93fc6 / P4.2h PR 552) that arm is gone; it now calls find-config-option, which is (find name *config-options* :key #'config-option-name :test #'string=) at src/config/registry.lisp:99-101 with no downcasing, while core-only-option-p (src/config/registry.lisp:112-115) still downcases. The two now disagree. Executed in the warm image (dev.sh eval, full package name BITCOIN-LISP.CONFIG): ("LogSourceLocations" known=NIL core-only=T find=NIL), ("logsourcelocations" known=T core-only=T), ("CheckBlockIndex" known=NIL core-only=T), ("DataDir" known=NIL), (parse-bitcoin-conf "LogSourceLocations=1") => (("logsourcelocations" . "1")). This reproduces the report's table exactly.

CONSEQUENCE — one correction, in the direction of MORE reachable but no more severe. The report enumerates three callers and calls the divergence unreachable; it missed a fourth: src/config/settings.lisp:151-154 (unknown-settings-keys -> unknown-config-file-keys -> known-config-option-p), driven from src/node/init.lisp:1566. settings.json keys are raw JSON keys and are never downcased (grep string-downcase src/config/settings.lisp: no hits). Executed: (unknown-settings-keys '(("LogSourceLocations" . "1") ("logsourcelocations" . "1") ("dbcache" . "100"))) => ("LogSourceLocations"). Pre-refactor that key was known=T and silent. So the reachable effect is exactly one spurious 'Ignoring unknown configuration value' warning for a hand-edited settings.json using a non-lowercase core-only spelling; unknown settings never abort startup (documented at src/config/settings.lisp:152-153, Core args.cpp:420-423).

The HARD-ERROR path the report flags as the reason to care is genuinely unreachable, as it says: check-cli-args builds name with an explicit (string-downcase ...) at src/config/args.lisp:140 before testing known-config-option-p at :174. The two log-only callers also receive downcased keys — split-option-token downcases at src/config/args.lisp:17, and parse-bitcoin-conf-sections returns all-lowercase keys (executed: sections/globals for a mixed-case conf came back as logsourcelocations/checkblockindex/logthreadnames).

MITIGATION worth recording: both the pre- and post-refactor docstrings declare the precondition 'T if NAME (lower-case, no dashes) is a recognized config option', so what the refactor dropped

