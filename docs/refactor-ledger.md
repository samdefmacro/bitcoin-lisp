# Refactor ledger

Companion to `docs/refactoring-plan-2026-08-27.md`. One row per merged
`refactor:` PR; the numbers are what `tests/structural-tests.lisp` measures
(the "Refactoring ratchets" section), so a row can be regenerated:

```
scripts/dev.sh eval '(list (bitcoin-lisp.tests::%duplicate-definition-names)
  (length (bitcoin-lisp.tests::%definitions-longer-than 200))
  (length (bitcoin-lisp.tests::%definitions-longer-than 100))
  (let ((text (bitcoin-lisp.tests::%source-text))) (mapcar (lambda (f) (cons f (bitcoin-lisp.tests::%serialization-family-count f text))) (list :stream-io :compact-size-definitions)))
  (reduce (function +) (bitcoin-lisp.tests::%bare-error-census) :key (function cdr))
  (bitcoin-lisp.tests::%layering-violations))'
```

Columns: dup = DEFUN/DEFMACRO names defined in two files; >200 / >100 = top-level
definitions over that many lines; stream / cs-defs = call sites of
the stream integer codecs and distinct CompactSize/varint definitions (the
retiring families; the `interop` column was dropped in P1.1 when buf-set-*
became the primitive layer under byte-buf rather than a second buffer); bare = `(error
"...")` forms; layer = upward package references; max file = longest file in
src/; cold = cold-battery check count / failures.

| date | PR | step | dup | >200 | >100 | stream | cs-defs | bare | layer | max file | cold |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 2026-08-27 | baseline (main 5f4f321) | — | 18 | 13 | 62 | 88 | 12 | 199 | 15 | 6,783 | 34,255 / 0 |
| 2026-08-27 | P0a | structural baselines | 18 | 13 | 62 | 88 | 12 | 199 | 15 | 6,783 | — |
| 2026-08-27 | P0b | fresh-FASL cold lane | 18 | 13 | 62 | 88 | 12 | 199 | 15 | 6,783 | 34,302 / 0 (fresh, 3m13s) |
| 2026-08-27 | P1.1 | one byte-buf/byte-reader (src/util/bytes.lisp) | 10 | 13 | 62 | 88 | 12 | 199 | 15 | 6,783 | 34,302 / 0 (fresh) |
| 2026-08-28 | P1.2 | CompactSize down to the bytes package (4 private variants gone) | 10 | 13 | 62 | 88 | 8 | 199 | 15 | 6,783 | 34,305 / 0 (fresh) |
| 2026-08-28 | P1.3 | last 10 duplicate names gone; redefinition warnings now fail the cold lane | 0 | 13 | 62 | 88 | 8 | 198 | 15 | 6,783 | 34,317 / 0 (fresh) |
| 2026-08-28 | P1.4 | package-local nicknames (bl.ser, bl.store, …) across src/ and tests/ | 0 | 13 | 62 | 88 | 8 | 198 | 15 | 6,783 | 34,319 / 0 (fresh) |
| 2026-08-28 | P1.5 | each module's package in src/<module>/package.lisp; src/package.lisp 1,780 → 246 lines | 0 | 13 | 62 | 88 | 8 | 198 | 15 | 6,783 | 34,319 / 0 (fresh) |
| 2026-08-28 | P2a | define-chain-params: 29 network dispatches in 8 files → one table (src/util/chainparams.lisp) | 0 | 13 | 62 | 88 | 8 | 200 | 15 | 6,783 | 34,420 / 0 (fresh) |
| 2026-08-28 | P2b-1 | messages.lisp (P2P) on byte-reader/byte-buf; nickname installer self-heals warm reloads | 0 | 13 | 62 | 42 | 8 | 199 | 15 | 6,783 | 34,420 / 0 (fresh) |
| 2026-08-28 | P2b-2 | define-message: 6 struct+reader+writer triples → 6 field lists (src/serialization/message-macro.lisp) | 0 | 13 | 62 | 42 | 8 | 202 | 15 | 6,783 | 34,420 / 0 (fresh) |
| 2026-08-28 | P2c | node-context: every P2P handler and the IBD pump take (peer payload ctx) instead of up to 8 params | 0 | 12 | 62 | 42 | 8 | 202 | 15 | 6,783 | 34,420 / 0 (fresh) |
| 2026-08-28 | P2d-1 | define-rpc: 161 handlers register themselves; register-all-methods 197 → 6 lines | 0 | 12 | 61 | 42 | 8 | 202 | 15 | 6,783 | 34,424 / 0 (fresh) |
| 2026-08-28 | P2e-1 | base-index + 9 generics: 3 per-index catch-ups → catch-up-index, 3 connect hooks → index-block-connected/disconnected (txindex still via :tx-index, P2e-2); start-node 1332 → 1227 (%start-txindex, %start-indexes) | 0 | 12 | 61 | 42 | 8 | 205† | 15 | 6,767 | 34,425 / 0 (fresh) |
| 2026-08-28 | P0c | cold lane gates on `undefined variable` warnings (scripts/check-undefined-variables.sh, self-tested); 6 docstrings cut short by an inner `"` fixed (open-coins-view-db, dispatch-rpc-method, %target-unroutable-p, apply-log-categories, %write-settings-file, recon-fanout-target-p) | 0 | 12 | 61 | 42 | 8 | 205 | 15 | 6,767 | 34,425 / 0 (fresh) |
| 2026-08-28 | P2e-2 | txindex joins node-indexes: the `:tx-index` argument threaded through 7 validation entry points, accept-downloaded-block, ibd-context, node-context and 4 RPC sites is gone (-263/+153 lines); index-height takes the chainstate; txindex rewinds its marker on disconnect; getindexinfo via the generics | 0 | 12 | 61 | 42 | 8 | 205 | 15 | 6,764 | 34,425 / 0 (fresh) |
| 2026-08-28 | P2f-1 | define-option registry (src/config-options.lisp): 4 option lists + an inline list of 15 → one table of 192 rows; known/repeatable/core-only/scalar/collected all answered from it | 0 | 12 | 61 | 42 | 8 | 205 | 15 | 6,764 | 34,432 / 0 (fresh) |
| 2026-08-28 | P2f-2 | option rows carry :global/:apply/:min; apply-config-globals 388 → 2 steps (table walk + a 113-line apply-parameter-interactions); registry mechanism split into src/option-registry.lisp, the table loads after the modules it names | 0 | 11 | 61 | 42 | 8 | 195 | 11 | 6,764 | 34,432 / 0 (fresh) |
| 2026-08-28 | P3.1 | node.lisp (6,477 lines) → src/node/ ×21 files by concern (params … init), pure move, same package; source-text tests read the directory | 0 | 11 | 61 | 42 | 8 | 195 | 11 | 6,753 | 34,453 / 0 (fresh) |
| 2026-08-28 | P3.2 | start-node 1,227 → 169 lines: 12 named init steps (%init-logging … %start-network-services) + %sync-thread-loop; each step takes only the keywords it reads | 0 | 11 | 63 | 42 | 8 | 195 | 11 | 6,753 | 34,453 / 0 (fresh) |
| 2026-08-28 | P3.3 | rpc/methods.lisp (6,753) → 8 files named after Core rpc/*.cpp; errors.lisp (36 error codes + rpc-error, first in the module, 2 duplicate defconstants dropped); core-tables.lisp (3 generated tables out of server.lisp); 11 RPCs moved to the file Core keeps them in | 0 | 11 | 63 | 42 | 8 | 195 | 11 | 4,029 | 34,453 / 0 (fresh); undefined-variable warnings 129 → 52 |
| 2026-08-28 | P3.4 | wallet → package bitcoin-lisp.wallet (bl.wallet), src/wallet/ ×9 files, 10 exports; money helpers to src/rpc/amounts.lisp; asd rpc → wallet → rpc-server; ~1,100 cross-package references qualified mechanically from a definition map; definition scanner taught `(bl.rpc:define-rpc ` | 0 | 11 | 63 | 42 | 8 | 195 | 11 | 4,029 | 34,455 / 0 (fresh) |
| 2026-08-28 | P3.2b | %sync-thread-loop 258 → 50 lines: %sync-thread-connect, %sync-pass, %sync-idle-tick (one tick of the message pump), %sync-offline-activation | 0 | 10 | 63 | 42 | 8 | 195 | 11 | 4,029 | 34,454 / 0 (fresh) |
| 2026-08-28 | P4.1 | condition hierarchy: bitcoin-lisp.conditions (bl.err) with bitcoin-lisp-error root, 8 per-module simple-error classes + signalling functions, consensus/policy errors with a reason; all 195 bare `(error "…")` sites classified (message text unchanged); 8 existing conditions re-parented; signalling functions declared non-returning; test-time format-arity scan of every site | 0 | 10 | 63 | 42 | 8 | 0 | 11 | 4,029 | 34,468 / 0 (fresh) |
| 2026-08-28 | P4.2a | ASDF sub-systems bitcoin-lisp/util and bitcoin-lisp/crypto; the main system :depends-on them, so an upward reference inside either is a compile error; layering test lists sub-systems first | 0 | 10 | 63 | 42 | 8 | 0 | 8 | 4,029 | 34,464 / 0 (fresh) |
| 2026-08-28 | P4.2b | bitcoin-lisp/logging: logging.lisp is package bitcoin-lisp.logging (bl.log); the main package :uses it and re-exports the 35 names the tree reaches, so no call site changed; tests name logging internals bl.log:: | 0 | 10 | 63 | 42 | 8 | 0 | 8 | 4,029 | 34,468 / 0 (fresh) |
| 2026-08-28 | P4.2c | bitcoin-lisp/kv: leveldb, flatfile, datadir and the fsync helpers are package bitcoin-lisp.kv (bl.kv) in src/kv/; storage :uses it and re-exports 72 names (computed from the image's real symbol set, not from a defun regex) | 0 | 10 | 63 | 42 | 8 | 0 | 8 | 4,029 | 34,469 / 0 (fresh) |
| 2026-08-28 | P4.2d | bitcoin-lisp/serialization as a sub-system (no package change: it never named anything above crypto); layering scanner ignores string literals (a docstring's `/bl:0.1.0/` was a false upward reference) | 0 | 10 | 63 | 42 | 8 | 0 | 6 | 4,029 | 34,469 / 0 (fresh) |
| 2026-08-28 | P4.2e | bitcoin-lisp/storage as a sub-system: *network* / network-magic moved down to chainparams, the 7 pruning knobs to storage/prune-policy.lisp (the node re-exports them), prune-old-blocks takes its target as an argument; storage no longer names the top package | 0 | 10 | 63 | 42 | 8 | 0 | 5 | 4,029 | 34,469 / 0 (fresh) |
| 2026-08-28 | P4.2f | bitcoin-lisp/net as a sub-system: the transport half of src/networking/ (fd-wait, minisketch, socks5, connection, v2-transport, peerdb, netaddress, addrman, torcontrol) loads before the main system; the protocol half stays above in the same package. *interrupt-check* moved down to util/context.lisp, compute-crc32 to kv/flatfile.lisp; config.lisp -> networking resolved (its 8 proxy/reachability knobs are transport) | 0 | 10 | 63 | 42 | 8 | 0 | 4 | 4,029 | 34,469 / 0 |
| 2026-08-28 | P4.2g | bitcoin-lisp/rpc-server as a sub-system: the JSON-RPC/HTTP server (package, errors, define-rpc, json, server) loads before the main system; the handlers, REST and the web UI stay above in the same package and register their HTTP surfaces (Core RegisterHTTPHandler) instead of being named by the server. Token bucket -> util/ratelimit.lisp (bl.rl), network-port/-dns-seeds/-rpc-port -> chainparams, the two RPC knobs -> rpc, the named-argument tables -> variables the server declares and core-tables.lisp fills, the wallet reads /wallet/<name> from *rpc-request-uri* (Core JSONRPCRequest::URI), the cookie directory through a generic function | 0 | 10 | 63 | 42 | 8 | 0 | 4 | 4,029 | 34,469 / 0 (fresh) |
| 2026-08-28 | P4.2h | bitcoin-lisp/config as a sub-system: the option registry (define-option) and the command-line, bitcoin.conf, settings.json and option-value parsers move from src/config.lisp + src/option-registry.lisp into src/config/{registry,values,args,conf,settings}.lisp, package bitcoin-lisp.config (bl.cfg), which the top package :uses; the option table and the node globals/glue stay above. config.lisp 1,530 -> 588 lines | 0 | 10 | 63 | 42 | 8 | 0 | 4 | 4,029 | 34,469 / 0 (fresh) |
| 2026-08-28 | P4.2i | Layering baseline 4 -> 0: the mempool never names validation while validation names the mempool (MemPoolAccept is in validation.cpp in Core too), so the mempool module now loads first; the four start-node glue functions (config-alist->start-node-plist, apply-config-globals, apply-parameter-interactions, args->start-node-plist -- Core InitParameterInteraction) leave config.lisp for src/node/args.lisp, after every layer they name. config.lisp 588 -> 312 lines; the layering positive control is a synthetic probe | 0 | 10 | 63 | 42 | 8 | 0 | 0 | 4,029 | 34,470 / 0 |
| 2026-08-28 | P4.3 | PAX manual: docs/manual.lisp grows from a 38-line placeholder to one section per layer/module (17), each with the Core files it mirrors, its entry points as PAX references (checked against the image by docs-check: a renamed function turns the manual red), invariants, traps and transcripts; nine packages gain :documentation; docs-check gains a second red gate for dangling references | 0 | 10 | 63 | 42 | 8 | 0 | 0 | 4,029 | 34,470 / 0; docs-check PASSED |
| 2026-08-28 | P5a | tests/support/ (package bitcoin-lisp.test-support, :USEd by the tests package): with-temp-directory / make-temp-directory replace seven per-file temp-directory fixtures (one of them a fixed path shared by parallel runs), with-network replaces the three regtest bindings and the mainnet one, make-test-node leaves rpc-tests.lisp -- 247 call sites, three cross-file load-order dependencies gone. New ratchet: the :: count over tests/ (7,174 in code) may only fall | 0 | 10 | 63 | 42 | 8 | 0 | 0 | 4,029 | 34,472 / 0 |
| 2026-08-28 | P5b | tests/support/{transactions,chain,mempool-fixtures,wallet}.lisp: the fourteen fixtures a SECOND test file already reached across for -- make-mempool-test-tx (10 files), regtest-node-fixture (9), make-reorg-test-block (6), make-test-chain-hashes (5), make-activate-block-fixture (4), the package fixture, the deterministic RNG, the wallet-chain node -- move out of whichever test file happened to define them and lose their % prefixes. Thirteen cross-file load-order dependencies gone; the :: count does not move (this step removes dependencies, not duplication) | 0 | 10 | 63 | 42 | 8 | 0 | 0 | 4,029 | 34,472 / 0 |
| 2026-08-29 | P5c | tests/ mirrors src/: 85 of the 97 test files move into the directory of the sub-system they would travel with if that layer were extracted (networking 18, storage 12, validation 12, coalton 10, mempool 7, crypto 6, wallet 6, serialization 4, util/kv/rpc 2 each, config/logging/mining/node 1 each); 12 stay at the top because they belong to no one module -- the ratchets, the cross-cutting suites, and the manual network scripts. git mv so history follows; the .asd keeps its exact order, only the component names gain a directory | 0 | 10 | 63 | 42 | 8 | 0 | 0 | 4,029 | 34,472 / 0 |
| 2026-08-29 | P6a | validate-block (307) splits into %check-block (107) and %contextual-check-block (191), making Core's CheckBlock / ContextualCheckBlock boundary a function boundary here too instead of a keyword flag. Measured every remaining long function against its Core counterpart first: only three of the ten are genuine splits, the rest mirror a Core function that is itself long (CreateTransactionInternal 376, ConnectBlock 379, DecodeScript 385) | 0 | 9 | 64 | 42 | 8 | 0 | 0 | 4,063 | 34,471 / 0 |
| 2026-08-29 | P6b | +long-function-baseline+ carries each entry's Core counterpart and verdict instead of a bare (name . lines): 2 split-pending (both need a state object first -- Core threads a 13-field Workspace through the MemPoolAccept phases), 6 exceptions where Core's own function is as long or longer, 1 borderline. The plan's "name the exceptions" clause, actually applied | 0 | 9 | 64 | 42 | 8 | 0 | 0 | 4,063 | 34,471 / 0 |
| 2026-08-29 | P6c | the two mempool script passes become %policy-script-checks (20) and %consensus-script-checks (29), Core's own names; validate-transaction-for-mempool 358 -> 330. NO metric moves -- and that is the finding: P6b had called this function a three-phase fusion, but the script passes were already delegated, so what remains is Core's PreChecks alone at 1.7x its length. Baseline annotation corrected | 0 | 9 | 64 | 42 | 8 | 0 | 0 | 4,063 | 34,471 / 0 |
| 2026-08-29 | P6d | perform-reorg (469) becomes a REORG state struct plus %reorg-disconnect (92), %reorg-connect (118) and %reorg-commit (114) -- Core's ActivateBestChainStep driving DisconnectTip / ConnectTip with a DisconnectedBlockTransactions pool. The struct is what made the split possible: eight accumulators are written in PHASE A and read in PHASE C, so the phases could not be lifted while they were let bindings | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,470 / 0 (fresh) |
| 2026-09-02 | A | second-round review (docs/refactoring-review-2026-09-02.md) wave A, the bug fixes: with-current-node-lock binds a gensym instead of the literal NODE (16 sites -- the review counted 18 -- none used the capture -- a test pins it); decodescript takes the segwit HRP from chain-params (was "tb"/"bc", wrong on testnet4/signet/regtest; segwit-hrp exported); the shutdown-token wait retries only EINTR instead of spinning on a closed pipe; a dead ignore-errors around a digit-guarded parse-integer dropped. The :: ratchet caught the three new internals at 7,177; six decodescript tests now share one helper and make-test-node takes :network -> 7,170 | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,476 / 1 (fresh; the 1 = the :: ratchet at 7,177) then 34,476 / 0 |
| 2026-09-02 | B1 | API by use: the 267 symbols one src package reached in another with `::` are exported from their home package (rpc 107 -- the wallet alone reached 660 rpc internals since the P3.4 split -- networking 53, top 44, validation 26, wallet 11, serialization 10, mempool 7, interop 6, storage 6, crypto 3), the 32 `%`-prefixed ones renamed without the prefix (`%btc` -> `satoshi->btc`, `%make-muhash` -> `make-muhash-raw` where the bare name was taken), and every `pkg::name` whose symbol was already external respelled `home:name` across src, tests and the manual. New ratchet `src-reaches-no-foreign-internals` = 0 with a positive control. Follow-ups noted by review: rpc/net.lisp reads ~28 peer/connection accessors for getpeerinfo (Core copies a CNodeStats struct -- a `peer-stats` seam belongs in wave D); `bl.rpc:script-type` is a second classifier beside `bl.val:classify-script` (wave D 1.2) | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,478 / 0 (fresh); docs-check PASSED; tests `::` 7,170 -> 4,454 |
| 2026-09-02 | B2 | tests/support gains with-ibd-context (19 hand-written LETs), signals-rpc-error (:code / :message / :exact-message -- 21 handler-case + fail + code-check sites; a bare form is refused at macroexpansion, since (signals bl.rpc:rpc-error ...) already accepts any error), rpc-error-code-of (the same 4-line helper defined twice as %rpc-error-code and wave10-rpc-code, 119 callers), and with-wallet-chain-node :wallet (8 identical createwallet prologues). Follow-ups from review: the 119 rpc-error-code-of sites are the macro in disguise; 13 more *ibd-context* LETs bind an existing ctx; 2 wallet prologues pass extra createwallet args | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,478 / 0 (fresh); tests `::` 4,454 -> 4,422 |
| 2026-09-02 | C1 | define-p2p-handler (src/networking/p2p-handlers.lisp): the 31-branch `string=` COND in handle-message and the second copy of the command list in check-peer-rate-limit become one table -- HANDLE-<command> of (peer payload ctx) plus :needs-mempool and :rate-bucket per row; 16 handler defuns became rows, 15 inline branches became small handlers; handle-message = log, rate limit, gethash, funcall. The handshake window (%await-verack) and the IBD block/headers override (dispatch-ibd-message) stay outside the table by design. Tests pin the 31 commands and a probe row proves registration reaches dispatch | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,544 / 0 (fresh); docs-check PASSED; tests `::` 4,422 |
| 2026-09-02 | C2 | define-wdb-key / define-wdb-value (src/wallet/wallet-store.lisp): 26 hand-written record codecs (key writers, key-field parsers, value writer/parser pairs) become 17 schema rows; the field kinds are DEFINE-MESSAGE's own table -- bl.ser:field-codec-forms (was %message-codec-forms) is now exported and shared -- and the generated code runs on byte-buf/byte-reader. The stream pair %wser/%wparse stays for the three CWalletTx codecs in wallet-tx / wallet-spend that feed a stream to the transaction codecs (follow-up: move them and retire the pair). The checksummed descriptor-key value, the xpub cache wrapper, the block locator and the conditional active-spk key stay hand-written | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,544 / 0 (fresh); tests `::` 4,422 |
| 2026-09-02 | C3 | src/serialization/messages.lisp: the three payload shapes that were copied between constructors become one builder each -- %nonce-payload (ping, pong), %locator-payload (getblocks, getheaders), %inv-list-payload (inv, getdata, notfound); seven constructors are now one line over the builder | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,544 / 0 |
| 2026-09-02 | D1 | src/validation/solver.lisp (Core script/solver.cpp): ONE scriptPubKey classifier. The looser classify-script (script.lisp), the faithful classify-output-script (transaction.lisp), the RPC's own script-type (the only one that knew P2A) and the descriptor code's script->address ladder become classify-script -- Solver's order, Solver's vSolutionsRet as the second value -- plus script-type-name; the Match* helpers and the P2SH/witness predicates move down beside it. Behaviour now Core's everywhere: a v0 program of odd length is nonstandard (was witness-unknown), OP_RETURN needs a push-only tail (was any OP_RETURN), a 33-byte push with a 04 header is not a pubkey, and getblock / decodescript / gettxout agree on "anchor". Review found two more copies -- rpc/rawtransaction.lisp's parse-multisig (a second multisig walk, now a wrapper over the classifier's data) and descriptors.lisp's %script-p2pk-p -- and that %match-multisig skipped Core's CPubKey::ValidSize header check on each key, now one shared %valid-pubkey-push-p. psbt.lisp's tapscript-leaf matcher and compressor.lisp's IsToKeyID family are Core's own separate tables and stay | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,567 / 0; docs-check PASSED; tests `::` 4,422 -> 4,417 |
| 2026-09-02 | D2 | chain-params carries Core's base58Prefixes (PUBKEY_ADDRESS, SCRIPT_ADDRESS, SECRET_KEY, EXT_PUBLIC_KEY, EXT_SECRET_KEY) and the SLIP-44 coin type; the address, WIF and BIP32 codecs read them by chain instead of branching on a two-valued :mainnet/:testnet, DecodeDestination-style (the version byte must be THIS chain's prefix). Ten constants, address-version-to-type, test-network-p and the dead *min-bip9-warning-heights* table go; wif-to-private-key returns the version byte instead of a made-up chain class; membership questions (is this byte some chain's WIF / xpub / xprv prefix?) are three predicates in chainparams rather than an exported list. chain-params-of-ext-prefix (first chain with the prefix) stays for the two descriptor sites that only hold a key's version byte, as Core's BIP32 provider does. New ratchet: the pseudo-network :testnet appears nowhere in src (positive control) | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,580 / 0 (fresh); docs-check PASSED; tests `::` 4,417 |
| 2026-09-02 | D3 | base-index (src/storage/index-base.lisp) owns the skeleton the four indexes each copied: open-index-db / close-index (the 14-line init clone x3 and four close functions), index-key (the prefix-byte + payload builders), the 36-byte height||hash marker codec with default index-best-block / index-set-best / index-clear-best methods (the blockfilterindex and coinstatsindex had character-identical encode/decode pairs and best/set/clear triples), one *index-meta-key* instead of four; a cache-share slot says which -dbcache line the DB draws on. The txindex (hash-only marker) and txospenderindex (hash||height) keep their own marker methods -- on-disk formats are not touched. Review: cache-share is a keyword with an ECASE, blockfilterindex-wipe reopens through open-index-db, the two marker layouts are documented | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,580 / 0 (fresh); docs-check PASSED |
| 2026-09-02 | E | the hot path and the trust boundaries: the byte-reader / byte-buf codecs in serialization/types.lisp carry (speed 3) (safety 1) and their argument types per function (they had no declaration at all beside fully declared primitives; a file-level DECLAIM was the first draft, until review pointed out it PROCLAIMs at load and leaks into every file compiled after); one octet-vector hash test in bl.bytes (octets= / octets-hash / make-octets-hash-table, mixing an outpoint's index bytes in -- the signature cache, the UTXO set and the IBD block tables had each invented it) replaces the mempool's seven EQUALP txid tables, the signature cache's and the IBD tables' private copies; the two (safety 0) blocks that read peer or file bytes are (safety 1); the ten struct<->Coalton codecs in interop.lisp had no caller and are gone. New ratchet: EQUALP hash tables in src (111 -> 103) may only fall, with a positive control. Benchmark (scripts/benchmark.sh, which now collects before each timing -- serialize-tx had measured 580 or 885 on identical trees depending on where a GC landed): serialize-tx 590 -> 570-580 ns/op, br-read-transaction 840 -> 760-870 across five runs (warm-image A/B on one heap: 575 vs 585 and 760 vs 840), signature-cache hit 600 -> 550, sha256 unchanged -- no regression, the read side a little faster | 0 | 8 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,587 / 0; tests `::` 4,417 |
| 2026-09-02 | F | Core's CValidationInterface as hook lists (src/util/validation-interface.lisp, bl.vi): validation/block.lisp announced nothing before -- it called the wallet, ZMQ, the index driver, -blocknotify, -stopatheight and the periodic flush by name, fourteen upward references into src/node/ and src/zmq.lisp the layering scanner could not see because the top package spans both ends of the load; the mempool did the same for ZMQ and the wallet. Now notify-block-connected / -disconnected / -updated-block-tip / -transaction-added / -removed, and the subscribers are define-validation-hook forms where they were defined (zmq.lisp, node/indexes, node/wallet-hooks, node/notify, node/housekeeping, node/flush). Behaviour: ZMQ, like Core's, no longer announces a background (assumeutxo) chainstate's blocks; -stopatheight guards the active chain itself; ZMQ's block notifications fire after the tip update (Core's order; they fired before). Three more, found by the result review (docs/refactor-result-review-2026-09-02.md §3.1) and kept: subscribers now run in one registration order for connect and disconnect -- zmq, indexes, wallet -- where the two paths used to disagree (Core registers ZMQ before the indexes and loads the wallet later); the reorg's forward-reconnected blocks now reach ZMQ too (the old %reorg-commit skipped it); and the hooks run AFTER the without-interrupts critical section that applies the UTXO set, writes the undo and moves the tip, so an interrupt in that window leaves an index one block behind the tip until its startup catch-up -- Core's indexes are asynchronous by design, and the old placement was stricter than Core. New scanner %top-package-upward-references resolves every bl:NAME to its DEFINING FILE (sb-introspect) and compares load positions -- the blind spot is closed; the remaining 77 upward references (nearly all the node struct's accessors and *node*, defined in src/node/state.lisp, last in the load, reached from rpc / wallet / networking / mining) are pinned as a baseline that may only shrink. connect-block fell under 200 lines and leaves the long-function baseline (>200 = 7) | 0 | 7 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,590 / 0 (fresh); docs-check PASSED; tests `::` 4,417 |
| 2026-09-02 | F2 | src/node/state.lisp (the node struct, *node*, the chainstate selectors) loads right after config.lisp instead of second in the LAST module: it needs only the packages and the storage layer, and every layer above validation reads the node's slots. The top-package upward baseline falls 77 -> 22; what remains is node machinery reached by name from rpc, networking and validation (snapshot chainstates, the flush budget, the disk-space gate, peer endpoints, shutdown), each a candidate to move down or announce. Side effect: the node struct's accessors now inline into the RPC code, which turned the three estimatesmartfee tests' `setf fdefinition` stub of node-fee-estimator from a warm-only failure into a cold one -- the stub sets the slot now, and the trio passes in both lanes for the first time | 0 | 7 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,590 / 0 (fresh); docs-check PASSED |
| 2026-09-02 | G | the sweep: 77 `defparameter +x+` become what their names say -- 32 numeric `defconstant`, 45 `alexandria:define-constant` (:test equalp) for strings, lists, vectors and computed values (a string DEFCONSTANT is a DEFCONSTANT-UNEQL at fasl load in a from-scratch build: the first draft had two and the fresh lane died in config/settings.lisp -- seven pre-existing string defconstants were converted with them), and the 9 that -option handling sets through the option table (`:global`) become `*x*` defvars; 30 vacuous `(t nil)` cond clauses, 17 `(not (null x))` and 36 `(if (null x) a b)` rewritten by a reader over the forms (review caught that swapping the branches left the comments between them on the wrong branch -- the 4 comment-bearing sites are back to their original text, lesson form-swap-must-carry-comments); the orphan-export baseline is one-way (a resolved entry fails until pruned); docs-check asserts every src/ directory and top-level file is named in the manual and the manual gains @zmq (the one undocumented module); the profile-tapscript watcher folds into profile-regions.sh (PROFILE_REGIONS), the eleven diag-* one-offs from 2026-05-15 and diag/ go. The five "unregistered test files" the review counted are git-ignored local scripts, never committed -- left alone. The undefined-variable gate now recognises a package-qualified definer (alexandria:define-constant): a constant referenced before its definition had always been tolerated as "defined somewhere", and the gate went red the moment its definer changed spelling | 0 | 7 | 66 | 42 | 8 | 0 | 0 | 4,114 | 34,591 / 0 (fresh); docs-check PASSED (coverage + its control); tests `::` 4,417 |
| 2026-09-02 | C4 | src/rpc/define-rpc.lisp: the handler lambda list names the method's positions -- `(node (hash (verbose :bool-or t)))` binds each positional parameter in Core's RPCHelpMan order, with six coercion kinds (:bool, :bool-or, :array, :or, :integer-or, :string-or) standing in for the `(let ((a (first params)) (b (positional-bool (second params)))) ...)` prologue; `params` stays bound for a handler that interprets a position itself (%parse-verbosity, the []-vs-null test). 47 of 162 handlers converted by a reader over the forms (the leading positional bindings of the opening LET become the spec, the rest of the LET stays), then the seven the review found half-converted finished by hand. The positional reads `(first params)`...`(nth N params)` in the converted files fall 136 -> 40. New ratchet `rpc-specs-stay-within-core-arity` (positive control in tree): a spec may not name more positions than Core's argument table in core-tables.lisp gives the method -- the checker reads that table from the source text, so the test adds no `::`. Not done, deliberately: the review's typed kinds (`:hex-hash`, a required `:string`/`:uint` raising +rpc-type-error+) -- 36 `(unless (stringp x) ...)` and ~9 non-negative-integer guards each carry a handler-specific message the functional suite asserts, so they convert one handler at a time against Core's RPCHelpMan text, not by a reader | 0 | 7 | 61 | 42 | 8 | 0 | 0 | 4,071 | 34,595 / 0 (fresh); docs-check PASSED; tests `::` 4,417 |
| 2026-09-03 | R1 | the result review's follow-ups (docs/refactor-result-review-2026-09-02.md §7, status in §8): ledger row F names the three behaviour changes wave F made without saying so, and the manual's util section explains why the hooks run after the critical section; `available-processor-count` falls back to 4 only when /proc/cpuinfo cannot be opened or read (any other error propagates), `prune-stale-undo-files` parses a filename explicitly and logs the garbage it deletes -- the review asked to keep such files, the pruning test documents deleting them, the test won; the seven hand-written wallet-record parses in wallet.lisp (lockedutxo, the two descriptor caches, descriptor, descriptor key/ckey, name/purpose, tx) call the parsers define-wdb-key already generated, and a new ratchet `wdb-schema-functions-are-called` (positive control; one documented write-only exception, the version record Core never reads back) keeps a generated reader from going uncalled again; 15 of 26 inline rpc-error handlers -> `signals-rpc-error`, 3 multi-line `*ibd-context*` lets -> `with-ibd-context`; the three comments wave G dropped are back on a documented `(t nil)`, the ten undocumented `(t nil)` clauses in case forms are gone (one landed its parens inside a trailing comment -- a reader that deletes a clause must look at the line before it), `(and stack (and stack t))` and the `%btc` comment fixed, the 2.1 count reads 16; `scripts/refactor/check-comments-kept.sh` lists comment text a batch rewrite dropped (per file, against a base revision) for the next reader to account for; functional-triage carries the p2p_sendheaders attribution (pre-existing) | 0 | 7 | 61 | 42 | 8 | 0 | 0 | 4,071 | 34,601 / 0 (structural 101 / 0; docs-check PASSED) |

## Notes

- The 15 layering violations (a file naming a package whose module loads
  later in `bitcoin-lisp.asd`): `config.lisp` → 7 packages (it loads second),
  `coalton/interop.lisp` → crypto/serialization/storage, `coalton/crypto.lisp`
  → crypto, `zmq.lisp` → serialization, and validation → mempool ×3
  (`validate-transaction-for-mempool` and friends). This is the "file →
  packages" table P4 works from; the config ones are the hard part.
- `ms-from-script` (243 lines) is a faithful port of Core's
  `miniscript.h` FromScript; it is on the >200 list so the decision to leave
  it is made in P3, not by default.

† 205 is the census on main as well — the earlier rows carried a stale 202; the P2e-1 diff adds and removes no `(error "...")` form.
